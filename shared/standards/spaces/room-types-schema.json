{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "draftsman-skills/shared/standards/spaces/room-types-schema.json",
  "title": "RoomTypeEntry",
  "description": "Per-entry metaschema for shared/standards/spaces/room-types/*.json files. Validates entries authored across the 13 room-type-applicable OmniClass Table 13 parent_category files (one per T13 level-2 parent code). NOTE: residential is intentionally absent — T13 (Spaces by Function) does not publish residential codes; residential spaces live in OmniClass Table 11 (Construction Entities by Function) which is deferred to a future sprint. Three construction-element parents (13-13 Void Areas, 13-15 Wall Spaces, 13-17 Encroachment Spaces) are also excluded as not Room.type-applicable — see _source/OmniClass-Table-13-source-notes.md §Phase X.B scope reduction.",
  "type": "object",
  "required": ["canonical_id", "omniclass_code", "parent_category", "_verification_status"],
  "properties": {
    "canonical_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*(\\.[a-z0-9_]+)+$",
      "description": "Snake_case dotted identifier used as Room.type in SkillInput. Format: parent_category.sub_category.entry_name. First segment matches the parent_category enum (snake_case names with underscores). Examples: healthcare_spaces.operating_theatre_general, education_and_training_spaces.classroom_general, parking_spaces.surface_lot, space_planning_types.planned_work_space."
    },
    "omniclass_code": {
      "type": "string",
      "pattern": "^13-[0-9]{2}( [0-9]{2}){0,4}$",
      "description": "OmniClass Table 13 verbatim 5-segment 13-digit code. Format: 13-XX XX XX XX XX. Sourced from declared public mirror per sprint-X-source-provenance.md."
    },
    "parent_category": {
      "type": "string",
      "enum": [
        "space_planning_types",
        "parking_spaces",
        "facility_service_spaces",
        "circulation_spaces",
        "education_and_training_spaces",
        "recreation_spaces",
        "government_spaces",
        "artistic_spaces",
        "museum_spaces",
        "library_spaces",
        "spiritual_spaces",
        "environmentally_controlled_spaces",
        "healthcare_spaces"
      ],
      "description": "Snake_case canonical name of the OmniClass Table 13 level-2 parent. One of the 13 room-type-applicable T13 parents (per pdfcoffee mirror 2026-06-05 + NIBS NBIMS-US V3 §2.4.4.3 wrapper). Three construction-element parents excluded (void_areas 13-13, wall_spaces 13-15, encroachment_spaces 13-17) — see source-notes §Phase X.B scope reduction. MUST match the level-2 segment of omniclass_code (e.g. parent_category=healthcare_spaces ↔ omniclass_code starts with 13-51)."
    },
    "parent_code": {
      "type": "string",
      "pattern": "^13-[0-9]{2}$",
      "description": "OPTIONAL convenience field — verbatim T13 level-2 parent code (e.g. '13-31' for education_and_training_spaces). Consumers can lookup by code OR by snake_case parent_category name. MUST equal the level-2 segment of omniclass_code when present."
    },
    "parent_path": {
      "type": "array",
      "items": {"type": "string"},
      "minItems": 1,
      "maxItems": 3,
      "description": "Hierarchical drill-down path (3-level max). E.g. [healthcare_spaces, treatment, operating_theatre]. First element MUST equal parent_category."
    },
    "common_aliases": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Non-canonical synonyms from architectural practice. Powers fuzzy-match lookup for orchestrators receiving non-canonical strings from drawing parsers. E.g. ['operating room', 'OR', 'theatre', 'surgery suite']."
    },
    "ifc_space_type": {
      "type": "string",
      "enum": ["PARKING", "GFA", "BERTHING", "EXTERNAL", "INTERNAL", "USERDEFINED", "NOTDEFINED"],
      "description": "Coarse IfcSpaceTypeEnum value per ISO 16739. Mostly INTERNAL (indoor) or EXTERNAL (outdoor)."
    },
    "_verification_status": {
      "type": "string",
      "enum": ["mirror_sourced", "occs_verified", "inferred"],
      "description": "Per-entry sourcing record. mirror_sourced=default (verbatim from declared mirror URL). occs_verified=cross-checked against OCCS canonical PDF (back-fill). inferred=synthesised from hierarchy (MUST honest-disclose in _inference_note)."
    },
    "_inference_note": {
      "type": "string",
      "description": "REQUIRED when _verification_status=inferred. Explains the inference (e.g. 'parent code 13-37 31 00 00 present in mirror; child code synthesised from OmniClass numbering pattern')."
    },
    "_source_mirror": {
      "type": "string",
      "format": "uri",
      "description": "URL of the mirror this entry was sourced from (per-entry override if not the primary declared in sprint-X-source-provenance.md)."
    },
    "cross_references": {
      "type": "object",
      "properties": {
        "bs_en_12464_1": {
          "type": ["string", "null"],
          "description": "Key path into shared/standards/lighting/BSEN12464/lux-levels.json (e.g. 'office.open_plan') OR null if no match."
        },
        "cibse_lg": {
          "type": ["string", "null"],
          "description": "Null until Sprint Y back-fills paid CIBSE source."
        },
        "ashrae_90_1": {
          "type": ["string", "null"],
          "description": "Key path into shared/standards/energy/ASHRAE-90-1/lpd-table-9-6-1.json OR null."
        },
        "ashrae_62_1": {
          "type": ["string", "null"],
          "description": "Key path into shared/standards/hvac/ASHRAE-62-1/ventilation-rates.json OR null."
        },
        "nrm2": {
          "type": ["string", "null"],
          "description": "Null until Sprint Y back-fills paid NRM2 source."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
