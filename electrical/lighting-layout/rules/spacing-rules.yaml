id: spacing-rules
description: Max spacing-to-height ratio rules
rules:
  - id: shr-check
    formula: "S ≤ SHR_max × Hm"
    default_shr_max: 1.5
    note: SHR_max from photometric data; 1.5 is conservative default
