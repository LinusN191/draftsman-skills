# Earthing v1.1.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship earthing v1.1.0 — adds a Kenya TN-S worked example, a new `calc.zs_loop_impedance` contract (WI3 deferred), and retrofits the existing 3 examples with the tool-deferral flag, without breaking the v1.0.0 IR contract.

**Architecture:** Additive-only schema extension at the IR root (`tool_call_pending_for_zs: boolean` + `zs_calc_tool_input: object`). Generator emits a complete replay payload that the future runtime tool will consume to refine LLM-estimated Zs values deterministically. KE jurisdiction is added as a first-class enum value alongside GB/EU/INT/US.

**Tech Stack:** JSON Schema draft-07 for IR validation; Python 3 + `json` stdlib for validation commands; YAML for evals and validator checks; Markdown for reasoning + schedule outputs.

**Spec:** `docs/superpowers/specs/2026-05-17-earthing-v1.1-design.md` (390 lines, 17 sections — full design rationale lives there)

**Pattern parents:** arc-flash v1.0.0 (WI3 tool deferral with `tool_call_pending` flag), cable-sizing v1.0.0 (calc contract + replay payload), fault-level v1.0.0 (IEC 60909 calc with jurisdiction branching), arc-flash-labelling v1.0.0 (multi-jurisdiction examples with explicit jurisdiction tags).

**File count:** ~22 files (8 new + 14 modified).

---

## File structure

### New files (8)

| Path | Responsibility |
|---|---|
| `shared/calculations/electrical/zs-loop-impedance.json` | Calc contract: inputs, outputs, jurisdiction branching, runtime contract |
| `electrical/earthing/validation/tool-deferral-shape.yaml` | 2 deterministic checks for tool_call_pending ↔ rationale.flags pair consistency |
| `electrical/earthing/examples/ke-nairobi-industrial-tn-s/input.json` | Site brief: KPLC TN-S 415V, 8 circuits, KS 1700 jurisdiction tag |
| `electrical/earthing/examples/ke-nairobi-industrial-tn-s/output.json` | Full IR with tool_call_pending_for_zs flag + zs_calc_tool_input replay payload |
| `electrical/earthing/examples/ke-nairobi-industrial-tn-s/intent-out.json` | Earthing intent payload for downstream consumers |
| `electrical/earthing/examples/ke-nairobi-industrial-tn-s/reasoning.md` | Engineer narrative — TN-S vs TN-C-S Ze split, Type D MCB rule, KE RCD policy |
| `electrical/earthing/examples/ke-nairobi-industrial-tn-s/sample-schedule.md` | 8-row markdown Zs verification schedule |
| `electrical/earthing/evals/eval-07-ke-tn-s-tool-deferral.yaml` | 11 assertions validating the KE example output |

### Modified files (14)

| Path | What changes |
|---|---|
| `shared/calculations/REQUIRED_TOOLS.json` | Add `calc.zs_loop_impedance` entry |
| `electrical/earthing/schemas/earthing-ir.schema.json` | Add KE to jurisdiction enum + 2 optional root fields |
| `electrical/earthing/prompts/generator.md` | Insert Step 9.5 + reshape Step 10 |
| `electrical/earthing/prompts/validator.md` | Add INV-9 (tool deferral shape) |
| `electrical/earthing/examples/uk-dwelling-tn-cs/output.json` | Add tool_call_pending_for_zs + zs_calc_tool_input + TOOL-CALL-PENDING flag |
| `electrical/earthing/examples/uk-dwelling-tn-cs/reasoning.md` | Add 1-paragraph v1.1 retrofit note at top |
| `electrical/earthing/examples/intl-rural-tt/output.json` | Same retrofit |
| `electrical/earthing/examples/intl-rural-tt/reasoning.md` | Same retrofit |
| `electrical/earthing/examples/us-commercial-nec/output.json` | Same retrofit |
| `electrical/earthing/examples/us-commercial-nec/reasoning.md` | Same retrofit |
| `electrical/earthing/skill.manifest.json` | Version 1.0.0 → 1.1.0; add new calc + example + eval + validator entries |
| `electrical/earthing/CHANGELOG.md` | Append v1.1.0 entry |
| `electrical/earthing/README.md` | Update jurisdiction table, eval coverage, "Tool calls awaiting runtime" table |
| `SKILLS_STATUS.md` | earthing row version + evals 6/3 → 7/3 + notes |

---

## Task 1 — Create calc.zs_loop_impedance contract

**Files:**
- Create: `shared/calculations/electrical/zs-loop-impedance.json`

- [ ] **Step 1: Create the calc contract file with the full content**

```json
{
  "$schema": "../../schemas/core/calculation.schema.json",
  "id": "iec-bs-zs-loop-impedance",
  "name": "Earth Fault Loop Impedance Verification (BS 7671 / IEC 60364 / NEC)",
  "executor": "tool",
  "tool_name": "calc.zs_loop_impedance",
  "formula_reference": "BS 7671:2018+A2:2022 Reg 411.3.2 + Table 41.2; IEC 60364-5-52:2009 Annex B (cable R+X); NFPA 70 Chapter 9 Table 9 (US cable R+X)",
  "implementation_note": "Computes Zs = Ze + (R1+R2) per circuit using per-CSA AC R+X from the jurisdiction-appropriate impedance table, then looks up Zs_max from BS 7671 Table 41.2 (BS/KE/EU/INT) or derives from NEC 5×In trip current (US). Returns a per-circuit verdict (pass / fail_needs_rcd / fail_oversize_cpc / pass_with_rcd). Inline LLM math diverges from the deterministic Python by ±15-25% due to imprecise per-CSA cable resistance estimates — this calc must NEVER be inline once the runtime tool ships.",
  "inputs": [
    {"id": "jurisdiction",                "type": "enum",  "values": ["GB", "EU", "INT", "KE", "US"], "description": "Selects impedance table (IEC part5-52 for non-US; NFPA70 Ch9 for US) and Zs_max source (BS Table 41.2 for non-US; NEC 5×In ratio for US)"},
    {"id": "supply",                      "type": "object", "description": "Service entry data: ze_declared_ohm, system_type (TN-S | TN-C-S | TT), voltage_phase_to_earth_v, frequency_hz"},
    {"id": "circuits",                    "type": "array",  "description": "Per-circuit data: circuit_id, cable_material (Cu|Al), cable_insulation (PVC|XLPE), line_csa_mm2_or_awg, cpc_csa_mm2_or_awg, route_length_m, device_type (MCB_B|MCB_C|MCB_D|BS_88_fuse|RCBO|GFCI), device_rating_a"},
    {"id": "operating_temperature_c",     "type": "int",    "default": 70, "description": "Conductor operating temperature for R uplift; 70°C for PVC, 90°C for XLPE; defaults to 70 if mixed"}
  ],
  "output": {
    "tool_version":      {"unit": "string",  "description": "Semver of the tool implementation"},
    "computed_at":       {"unit": "ISO-8601","description": "Timestamp when the tool produced this result"},
    "sources":           {"unit": "array",   "description": "Standards file paths consulted by the tool"},
    "results":           {"unit": "array",   "description": "Per-circuit verdict array — see runtime_contract"},
    "overall_compliant": {"unit": "boolean", "description": "True if every circuit verdict is pass or pass_with_rcd"}
  },
  "runtime_contract": {
    "request":  "{ tool_name: 'calc.zs_loop_impedance', inputs: { jurisdiction, supply, circuits, operating_temperature_c } }",
    "response": "{ ok: true, result: { tool_version, computed_at, sources, results: [{ circuit_id, r1_per_km_ohm, r2_per_km_ohm, r1_plus_r2_ohm, zs_calc_ohm, zs_max_ohm, zs_max_source, margin_pct, verdict, rcd_required }], overall_compliant }, error?: string }"
  },
  "jurisdiction_branching": {
    "GB_KE_EU_INT": {
      "impedance_table": "shared/standards/electrical/IEC60364/part5-52-cable-impedance.json",
      "zs_max_table":   "shared/standards/electrical/BS7671/reg411-disconnection-times.json (table_41_2_MCB_Zs_max_ohms + table_41_3_BS_88_fuse_Zs_max_ohms)",
      "ks_1700_note":   "KS 1700:2018 Annex E reproduces BS 7671 Table 41.2 verbatim — KE routes through the BS table"
    },
    "US": {
      "impedance_table": "shared/standards/electrical/NFPA70/chapter9-tables.json::Table_9_Alternating_Current_Resistance_and_Reactance_for_600V_Cables",
      "zs_max_derivation": "NEC has no fixed Zs_max table — verdict computed from instantaneous trip current Ia = 5×In (Type C) or 10×In (Type D) with 277V phase-to-ground or 120V/240V depending on system"
    }
  },
  "adverse_condition_factor": "BS 7671 Section 411.3.2 implies a 0.8 factor on Zs_max already baked into Table 41.2 values (cable temperature rise at fault). Tool does NOT re-apply.",
  "verdict_logic": {
    "pass":               "zs_calc <= zs_max (no RCD required by Zs)",
    "fail_needs_rcd":     "zs_calc > zs_max — RCD required to achieve required disconnection time",
    "fail_oversize_cpc":  "zs_calc > zs_max even with RCD threshold — must oversize CPC",
    "pass_with_rcd":      "zs_calc <= zs_max but RCD additionally required (e.g. socket circuit ≤32A per BS 7671 411.3.3 / KS 1700 411.3.3)"
  },
  "_consuming_skills": [
    "electrical/earthing (primary, since v1.1.0)",
    "electrical/db-layout (selectivity verification, future)",
    "electrical/cable-sizing (Zs as design constraint, future)"
  ]
}
```

- [ ] **Step 2: Validate JSON**

Run: `python3 -c "import json; json.load(open('shared/calculations/electrical/zs-loop-impedance.json')); print('OK')"`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add shared/calculations/electrical/zs-loop-impedance.json
git commit -m "$(cat <<'EOF'
feat(earthing): calc.zs_loop_impedance contract — Zs verification per BS 7671 / IEC 60364 / NEC

WI3 tool-call deferred until runtime ships. Jurisdiction-branching:
GB/KE/EU/INT use IEC part5-52 cable impedance + BS Table 41.2 Zs_max
(KS 1700 reproduces BS Table 41.2 verbatim). US uses NFPA70 Ch9 + NEC
trip-current derivation.

First consumer: electrical/earthing v1.1.0. Future consumers:
db-layout selectivity, cable-sizing constraint.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2 — Register tool in REQUIRED_TOOLS.json

**Files:**
- Modify: `shared/calculations/REQUIRED_TOOLS.json`

- [ ] **Step 1: Read the current file**

Run: `python3 -c "import json; d=json.load(open('shared/calculations/REQUIRED_TOOLS.json')); print('current tools:', [t['tool_name'] for t in d['tools']])"`

Expected: List of existing tools — `calc.zs_loop_impedance` MUST NOT be in the list yet.

- [ ] **Step 2: Add the new tool entry**

Use the Edit tool. Find the last entry in the `tools` array (currently the most recently added tool — likely `calc.render_label` or `calc.diversity_factor`). After its closing `}`, insert a comma and the new entry:

```json
,
    {
      "tool_name": "calc.zs_loop_impedance",
      "registered_for": "shared/calculations/electrical/zs-loop-impedance.json",
      "spec_summary": "Earth fault loop impedance Zs per circuit. Reads IEC 60364-5-52 Annex B (or NFPA70 Ch9 for US) for cable R+X, BS 7671 Table 41.2 (or NEC 5×In derivation) for Zs_max, returns per-circuit verdict.",
      "owner": "draftsman-runtime",
      "since_version": "1.1.0"
    }
```

- [ ] **Step 3: Validate**

Run: `python3 -c "import json; d=json.load(open('shared/calculations/REQUIRED_TOOLS.json')); assert any(t['tool_name']=='calc.zs_loop_impedance' for t in d['tools']); print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add shared/calculations/REQUIRED_TOOLS.json
git commit -m "$(cat <<'EOF'
feat(REQUIRED_TOOLS): register calc.zs_loop_impedance

Adds runtime registration for the earthing v1.1.0 Zs verification
tool. Implementation owned by draftsman-runtime.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3 — Extend earthing-ir.schema.json (jurisdiction KE + 2 root fields)

**Files:**
- Modify: `electrical/earthing/schemas/earthing-ir.schema.json`

- [ ] **Step 1: Add KE to jurisdiction enum**

Use the Edit tool. Replace exactly:

```json
    "jurisdiction": {
      "enum": ["GB", "EU", "INT", "US"],
      "description": "Selects which standards files were loaded at generation time"
    },
```

With:

```json
    "jurisdiction": {
      "enum": ["GB", "EU", "INT", "KE", "US"],
      "description": "Selects which standards files were loaded at generation time. KE (Kenya, KS 1700:2018) routes through BS 7671 Table 41.2 Zs_max + IEC 60364-5-52 cable impedance per KS 1700 Annex E."
    },
```

- [ ] **Step 2: Add tool_call_pending_for_zs + zs_calc_tool_input at IR root**

Use the Edit tool. Replace exactly:

```json
    "drawing_notes":     { "type": "array", "items": { "type": "string" } },
    "drawn_as_symbols":  { "type": "array", "items": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" }, "description": "Roll-up of IEC 60617 symbol_ids — every entry must resolve in IEC60617/symbol-index.json" },
    "flags":             { "type": "array", "items": { "type": "string" } },
    "rationale":         { "$ref": "../../../shared/schemas/core/rationale.schema.json" }
  }
}
```

With:

```json
    "drawing_notes":     { "type": "array", "items": { "type": "string" } },
    "drawn_as_symbols":  { "type": "array", "items": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" }, "description": "Roll-up of IEC 60617 symbol_ids — every entry must resolve in IEC60617/symbol-index.json" },
    "flags":             { "type": "array", "items": { "type": "string" } },
    "tool_call_pending_for_zs": {
      "type": "boolean",
      "description": "WI3 deferral flag. True until calc.zs_loop_impedance runtime tool ships. When true, circuits[].zs_ohm and zs_max_ohm are LLM-computed estimates with disclaimer in rationale.flags. When false or absent, values are tool-computed."
    },
    "zs_calc_tool_input": {
      "type": "object",
      "description": "The payload that would be sent to calc.zs_loop_impedance once runtime ships. Captured at generation time so the runtime can replay deterministically without re-prompting the LLM.",
      "required": ["jurisdiction", "supply", "circuits", "operating_temperature_c"],
      "properties": {
        "jurisdiction": { "enum": ["GB", "EU", "INT", "KE", "US"] },
        "supply": {
          "type": "object",
          "required": ["ze_declared_ohm", "system_type", "voltage_phase_to_earth_v", "frequency_hz"],
          "properties": {
            "ze_declared_ohm":          { "type": "number", "minimum": 0 },
            "system_type":              { "enum": ["TN-S", "TN-C-S", "TT"] },
            "voltage_phase_to_earth_v": { "type": "integer", "enum": [120, 230, 240, 277] },
            "frequency_hz":             { "type": "integer", "enum": [50, 60] }
          }
        },
        "circuits": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["circuit_id", "cable_material", "cable_insulation", "line_csa_mm2_or_awg", "cpc_csa_mm2_or_awg", "route_length_m", "device_type", "device_rating_a"],
            "properties": {
              "circuit_id":             { "type": "string" },
              "cable_material":         { "enum": ["Cu", "Al"] },
              "cable_insulation":       { "enum": ["PVC", "XLPE"] },
              "line_csa_mm2_or_awg":    { "type": "string" },
              "cpc_csa_mm2_or_awg":     { "type": "string" },
              "route_length_m":         { "type": "number", "exclusiveMinimum": 0 },
              "device_type":            { "enum": ["MCB_B", "MCB_C", "MCB_D", "BS_88_fuse", "RCBO", "GFCI"] },
              "device_rating_a":        { "type": "integer", "minimum": 1 }
            }
          }
        },
        "operating_temperature_c":      { "type": "integer", "minimum": 20, "maximum": 120 }
      }
    },
    "rationale":         { "$ref": "../../../shared/schemas/core/rationale.schema.json" }
  }
}
```

- [ ] **Step 3: Validate JSON syntax**

Run: `python3 -c "import json; json.load(open('electrical/earthing/schemas/earthing-ir.schema.json')); print('schema OK')"`
Expected: `schema OK`

- [ ] **Step 4: Validate v1.0.0 examples still pass v1.1 schema (backward compat)**

Run:
```bash
python3 <<'PY'
import json, jsonschema
schema = json.load(open('electrical/earthing/schemas/earthing-ir.schema.json'))
for example in ['uk-dwelling-tn-cs', 'intl-rural-tt', 'us-commercial-nec']:
    data = json.load(open(f'electrical/earthing/examples/{example}/output.json'))
    try:
        jsonschema.validate(instance=data, schema=schema)
        print(f'{example}: PASS')
    except Exception as e:
        print(f'{example}: FAIL — {e}')
PY
```

Expected: 3 `PASS` lines. If `jsonschema` is not installed, run `pip install jsonschema` first.

- [ ] **Step 5: Commit**

```bash
git add electrical/earthing/schemas/earthing-ir.schema.json
git commit -m "$(cat <<'EOF'
feat(earthing): IR schema v1.1 — add KE jurisdiction + tool_call_pending_for_zs + zs_calc_tool_input

Additive-only changes. v1.0.0 outputs remain valid against v1.1
schema (verified against all 3 existing examples).

- jurisdiction enum gains KE (Kenya, KS 1700:2018)
- tool_call_pending_for_zs: boolean at IR root
- zs_calc_tool_input: object at IR root carrying the calc.zs_loop_impedance replay payload

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4 — Create tool-deferral-shape validator YAML

**Files:**
- Create: `electrical/earthing/validation/tool-deferral-shape.yaml`

- [ ] **Step 1: Create the validator file**

```yaml
# Earthing v1.1 — tool deferral shape consistency
#
# Verifies that the tool_call_pending_for_zs flag is consistent with the
# TOOL-CALL-PENDING:calc.zs_loop_impedance string in rationale.flags.
#
# Both must be present together, or both must be absent. Mismatched pairs
# indicate an authoring error.

name: tool-deferral-shape
applies_to: electrical/earthing
schema_version: 1.1.0

checks:
  - id: tool_deferral_pair_consistent
    severity: critical
    description: "When tool_call_pending_for_zs is true, zs_calc_tool_input + TOOL-CALL-PENDING flag must both be present"
    rule: |
      if ir.tool_call_pending_for_zs == true then
        ir.zs_calc_tool_input must exist AND
        ir.rationale.flags must include string matching ^TOOL-CALL-PENDING:calc\.zs_loop_impedance

  - id: tool_deferral_no_orphan_flag
    severity: critical
    description: "TOOL-CALL-PENDING:calc.zs_loop_impedance flag must not appear without tool_call_pending_for_zs being true"
    rule: |
      if ir.rationale.flags includes string matching ^TOOL-CALL-PENDING:calc\.zs_loop_impedance then
        ir.tool_call_pending_for_zs == true
```

- [ ] **Step 2: Commit**

```bash
git add electrical/earthing/validation/tool-deferral-shape.yaml
git commit -m "$(cat <<'EOF'
feat(earthing): tool-deferral-shape validator — 2 critical checks

Ensures tool_call_pending_for_zs flag and TOOL-CALL-PENDING string in
rationale.flags are mutually consistent. Critical severity (mismatched
pairs indicate authoring error).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5 — Update generator + validator prompts

**Files:**
- Modify: `electrical/earthing/prompts/generator.md`
- Modify: `electrical/earthing/prompts/validator.md`

- [ ] **Step 1: Read current generator.md to locate insertion point**

Run: `grep -n "^## Step\|^### Step" electrical/earthing/prompts/generator.md`
Expected: Numbered step headings — Step 9 (CPC sizing) should precede Step 10 (Zs verification). Insertion happens between them.

- [ ] **Step 2: Insert Step 9.5 in generator.md**

Use the Edit tool to find the line containing the heading for Step 10 (something like `## Step 10 — Verify Zs against Table 41.2/41.3` or similar). Insert the following block immediately BEFORE that line:

```markdown
## Step 9.5 — Build the calc.zs_loop_impedance request payload (WI3 deferred)

Assemble the `zs_calc_tool_input` object at the IR root. This is the snapshot the runtime tool will consume once shipped (per WI3 — see `shared/calculations/electrical/zs-loop-impedance.json` for the contract).

Required structure:

```json
{
  "zs_calc_tool_input": {
    "jurisdiction": "<from inputs: GB | EU | INT | KE | US>",
    "supply": {
      "ze_declared_ohm": <from supply data>,
      "system_type": "<TN-S | TN-C-S | TT>",
      "voltage_phase_to_earth_v": <230 | 240 | 277 | 120>,
      "frequency_hz": <50 | 60>
    },
    "circuits": [
      {
        "circuit_id": "C01",
        "cable_material": "Cu | Al",
        "cable_insulation": "PVC | XLPE",
        "line_csa_mm2_or_awg": "<e.g. 2.5mm² or 12 AWG>",
        "cpc_csa_mm2_or_awg": "<e.g. 1.5mm² or 12 AWG>",
        "route_length_m": <float>,
        "device_type": "MCB_B | MCB_C | MCB_D | BS_88_fuse | RCBO | GFCI",
        "device_rating_a": <int>
      }
    ],
    "operating_temperature_c": 70
  }
}
```

Notes:
- `operating_temperature_c` defaults to 70 for PVC; use 90 for XLPE; default to 70 if mixed
- KE jurisdiction routes through BS 7671 Table 41.2 (KS 1700 Annex E adopts it verbatim) + IEC 60364-5-52 Annex B for cable impedance
- US routes through NFPA 70 Chapter 9 Table 9 + NEC 5×In trip-current derivation (no fixed Zs_max table)
- Populate ALL circuits from the design; the tool processes the full batch in one call

```

- [ ] **Step 3: Reshape Step 10 in generator.md**

Find the existing Step 10 heading and the paragraph(s) below it. Replace the heading + body with:

```markdown
## Step 10 — Compute Zs and verdict inline, with WI3 disclaimer

Since `calc.zs_loop_impedance` is not yet runtime-shipped (WI3 deferral), compute Zs per circuit inline using rough R1+R2 estimates from the cable CSA + length. Apply the appropriate Zs_max lookup:

- **GB / KE / EU / INT:** BS 7671:2018+A2 Table 41.2 (MCB) or Table 41.3 (BS 88 fuse) — KS 1700 Annex E adopts BS Table 41.2 verbatim, so KE uses the same values
- **US:** Derive from NEC 5×In trip current (Type C) or 10×In (Type D) — there is no fixed Zs_max table

Required actions every generation:

1. Set `tool_call_pending_for_zs: true` at the IR root
2. Populate each `circuits[].{zs_ohm, zs_max_ohm, zs_compliance, rcd_required}` field with your inline estimate (these become the LLM-computed disclaimer values)
3. Append exactly this string to `rationale.flags`:
   `"TOOL-CALL-PENDING:calc.zs_loop_impedance — Zs values are LLM-estimates; deterministic refinement deferred per WI3."`

When the runtime tool ships in v2.0.0:
- Step 10 becomes: invoke the tool with `zs_calc_tool_input`, populate `circuits[]` from the tool response, set `tool_call_pending_for_zs: false`, remove the TOOL-CALL-PENDING flag
- Schema v2.0.0 will drop the deferral flags

```

- [ ] **Step 4: Add INV-9 to validator.md**

Read current validator.md to locate the INV list (look for existing INV-1, INV-2, etc.). Append a new INV at the end of the list:

```markdown
## INV-9 — Tool deferral shape (NEW in v1.1.0)

When `tool_call_pending_for_zs: true`:
- `zs_calc_tool_input` MUST be present and well-formed per the schema in `electrical/earthing/schemas/earthing-ir.schema.json`
- `rationale.flags` MUST contain a string matching `^TOOL-CALL-PENDING:calc\.zs_loop_impedance`

When `tool_call_pending_for_zs: false` or absent:
- The TOOL-CALL-PENDING string MUST NOT be present in `rationale.flags`
- `circuits[].zs_ohm` values must reflect tool output (not LLM estimates)

Severity: CRITICAL. Mismatched pairs indicate an authoring error that breaks the runtime contract.
```

- [ ] **Step 5: Validate insertions**

Run:
```bash
grep -c "Step 9.5" electrical/earthing/prompts/generator.md
grep -c "tool_call_pending_for_zs: true" electrical/earthing/prompts/generator.md
grep -c "INV-9" electrical/earthing/prompts/validator.md
```
Expected: each returns `1` or higher.

- [ ] **Step 6: Commit**

```bash
git add electrical/earthing/prompts/generator.md electrical/earthing/prompts/validator.md
git commit -m "$(cat <<'EOF'
feat(earthing): prompts — Step 9.5 (build replay payload) + reshaped Step 10 + INV-9

Generator emits zs_calc_tool_input alongside inline Zs estimates; sets
tool_call_pending_for_zs flag + TOOL-CALL-PENDING string in
rationale.flags per WI3 deferral. Validator gains INV-9 to enforce
flag/string pair consistency.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6 — Create KE example input.json

**Files:**
- Create: `electrical/earthing/examples/ke-nairobi-industrial-tn-s/input.json`

- [ ] **Step 1: Create directory + file**

```bash
mkdir -p electrical/earthing/examples/ke-nairobi-industrial-tn-s
```

- [ ] **Step 2: Write input.json with the full content**

```json
{
  "project_id": "ke-nairobi-enterprise-light-engineering",
  "site_description": "Light engineering workshop in Nairobi Industrial Area (Enterprise Road), 1980s build. Three-phase mixed-use: workshop machine tools, office mezzanine, lighting, small power, compressor. Mains intake via KPLC Wayleave NRB-IND-2143.",
  "jurisdiction": "KE",
  "standards_stack": {
    "primary": "KS 1700:2018 (Code of Practice for Electrical Wiring of Buildings)",
    "adopted_by_reference": "BS 7671:2018+A2:2022 (where KS 1700 defers; KS 1700 Annex E reproduces BS Table 41.2)",
    "equipment_grounding": "IEC 60364-5-54 (CPC sizing fallback)"
  },
  "supply": {
    "dno": "KPLC (Kenya Power & Lighting Company)",
    "wayleave_reference": "KPLC-NRB-IND-2143",
    "connection_year": 1987,
    "system_type": "TN-S",
    "voltage": "415V/240V 3-phase 4-wire 50 Hz",
    "ze_declared_ohm": 0.80,
    "ze_provenance": "Measured at intake position 2026-03-12 by KPLC technician; degraded from original 0.65 Ω (1987 commissioning) due to sheath corrosion on the separate-Earth cable",
    "main_switch": "100A TPN with shrouded busbars (BS EN 60947-3)",
    "pfc_declared_ka": 9.8
  },
  "circuits": [
    {"circuit_id": "C01", "description": "Lighting workshop (LED highbays, 8 × 150W)", "phase": "L1", "device": "10A MCB Type B", "cable": "1.5mm² T+E PVC Cu", "route_length_m": 35, "cpc_size": "1.0mm²"},
    {"circuit_id": "C02", "description": "Lighting office mezzanine + stairwell", "phase": "L2", "device": "6A MCB Type B", "cable": "1.5mm² T+E PVC Cu", "route_length_m": 22, "cpc_size": "1.0mm²"},
    {"circuit_id": "C03", "description": "Socket ring main workshop", "phase": "L3", "device": "32A MCB Type B", "cable": "2.5mm² T+E PVC Cu", "route_length_m": 28, "cpc_size": "1.5mm²"},
    {"circuit_id": "C04", "description": "Socket radials office", "phase": "L1", "device": "20A MCB Type B", "cable": "2.5mm² T+E PVC Cu", "route_length_m": 18, "cpc_size": "2.5mm²"},
    {"circuit_id": "C05", "description": "Lathe 3-ph 5.5kW", "phase": "L1+L2+L3", "device": "20A MCB Type C", "cable": "2.5mm² SY 4-core PVC Cu", "route_length_m": 15, "cpc_size": "2.5mm²"},
    {"circuit_id": "C06", "description": "Pillar drill 3-ph 2.2kW", "phase": "L1+L2+L3", "device": "16A MCB Type C", "cable": "1.5mm² SY 4-core PVC Cu", "route_length_m": 12, "cpc_size": "1.5mm²"},
    {"circuit_id": "C07", "description": "Compressor 3-ph 7.5kW", "phase": "L1+L2+L3", "device": "25A MCB Type D", "cable": "4mm² SY 4-core PVC Cu", "route_length_m": 25, "cpc_size": "4mm²"},
    {"circuit_id": "C08", "description": "Submain to gate house", "phase": "L1+N+CPC", "device": "40A MCB Type B", "cable": "10mm² SWA PVC Cu", "route_length_m": 60, "cpc_size": "6mm² SWA armour"}
  ],
  "extraneous_parts": ["metal_water_service", "metal_gas_service", "structural_steel_columns"],
  "supplementary_bonding_locations": ["wash_room"],
  "db_designations": ["MSP-100"]
}
```

- [ ] **Step 3: Validate JSON**

Run: `python3 -c "import json; json.load(open('electrical/earthing/examples/ke-nairobi-industrial-tn-s/input.json')); print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add electrical/earthing/examples/ke-nairobi-industrial-tn-s/input.json
git commit -m "$(cat <<'EOF'
feat(earthing): KE Nairobi industrial TN-S example — input.json

Light engineering workshop, KPLC TN-S 415V supply, Ze=0.80Ω, 8
circuits including Type D motor MCBs and a 60m submain that
exercises the Zs verification flow at the limit of compliance.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7 — Create KE example intent-out.json

**Files:**
- Create: `electrical/earthing/examples/ke-nairobi-industrial-tn-s/intent-out.json`

- [ ] **Step 1: Read the intent schema to confirm shape**

Run: `cat electrical/earthing/schemas/earthing-intent.schema.json | head -60`

Confirm the required fields. Typical earthing intent shape: `intent_type`, `intent_version`, `produced_by_skill_version`, `nodes` or `system_type` + `circuits[]`.

- [ ] **Step 2: Write intent-out.json**

```json
{
  "intent_type": "earthing",
  "intent_version": "1.0.0",
  "produced_by_skill_version": "earthing/1.1.0",
  "tool_call_pending": true,
  "jurisdiction": "KE",
  "system_type": "TN-S",
  "ze_declared_ohm": 0.80,
  "supply_bond_type": "tn_s_separate_pe",
  "main_earthing_conductor_csa_mm2": 16,
  "electrodes": [],
  "main_bonding": [
    { "id": "B1", "target": "metal_water_service", "csa_mm2": 10 },
    { "id": "B2", "target": "metal_gas_service",   "csa_mm2": 10 },
    { "id": "B3", "target": "structural_steel",    "csa_mm2": 6  }
  ],
  "circuits": [
    { "circuit_id": "C01", "cpc_csa_mm2": 1.0, "device": "MCB_B_10A", "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C02", "cpc_csa_mm2": 1.0, "device": "MCB_B_6A",  "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C03", "cpc_csa_mm2": 1.5, "device": "MCB_B_32A", "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C04", "cpc_csa_mm2": 2.5, "device": "MCB_B_20A", "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C05", "cpc_csa_mm2": 2.5, "device": "MCB_C_20A", "zs_compliance": "pass",          "rcd_required": false },
    { "circuit_id": "C06", "cpc_csa_mm2": 1.5, "device": "MCB_C_16A", "zs_compliance": "pass",          "rcd_required": false },
    { "circuit_id": "C07", "cpc_csa_mm2": 4.0, "device": "MCB_D_25A", "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C08", "cpc_csa_mm2": 6.0, "device": "MCB_B_40A", "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_sensitivity_ma": 30 }
  ],
  "overall_compliant": true,
  "produced_at": "2026-05-18"
}
```

- [ ] **Step 3: Validate JSON**

Run: `python3 -c "import json; json.load(open('electrical/earthing/examples/ke-nairobi-industrial-tn-s/intent-out.json')); print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add electrical/earthing/examples/ke-nairobi-industrial-tn-s/intent-out.json
git commit -m "$(cat <<'EOF'
feat(earthing): KE Nairobi example — intent-out.json

Slim earthing intent payload for downstream consumers (cable-containment,
db-layout future selectivity checks).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8 — Create KE example output.json (full IR)

**Files:**
- Create: `electrical/earthing/examples/ke-nairobi-industrial-tn-s/output.json`

- [ ] **Step 1: Write output.json with the full content**

```json
{
  "$schema": "../../schemas/earthing-ir.schema.json",
  "drawing_type": "earthing_schematic",
  "version": "1.1.0",
  "meta": {
    "project_id": "ke-nairobi-enterprise-light-engineering",
    "skill_version": "earthing/1.1.0",
    "produced_at": "2026-05-18T10:00:00Z",
    "consumed_intents": []
  },
  "jurisdiction": "KE",
  "earthing_system": {
    "system_type": "TN-S",
    "code_clause": "KS 1700:2018 §411.4 (TN-S system; adopts BS 7671:2018+A2 Reg 411.4)"
  },
  "met": {
    "location": "main switchroom adjacent to KPLC intake, ground floor",
    "supply_bond_type": "tn_s_separate_pe",
    "main_earthing_conductor_csa_mm2": 16
  },
  "electrodes": [],
  "main_bonding": [
    { "id": "B1", "source": "MET", "target": "metal_water_service",  "csa_mm2_or_awg": "10mm²", "code_clause": "BS 7671:2018+A2 Reg 544.1.1 (adopted by KS 1700)" },
    { "id": "B2", "source": "MET", "target": "metal_gas_service",    "csa_mm2_or_awg": "10mm²", "code_clause": "BS 7671:2018+A2 Reg 544.1.1 (adopted by KS 1700)" },
    { "id": "B3", "source": "MET", "target": "structural_steel",     "csa_mm2_or_awg": "6mm²",  "code_clause": "BS 7671:2018+A2 Reg 544.1.1 (adopted by KS 1700)" }
  ],
  "supplementary_bonding": [
    {
      "location": "other",
      "items_bonded": ["wash_room_taps", "wash_room_metal_partition"],
      "csa_mm2_or_awg": "4mm²",
      "code_clause": "BS 7671:2018+A2 Reg 415.2 (adopted by KS 1700 §415)"
    }
  ],
  "circuits": [
    { "circuit_id": "C01", "db_designation": "MSP-100", "voltage_class": "LV_power", "breaker_rating_a": 10, "breaker_curve": "B", "route_length_m": 35, "cpc_csa_mm2_or_awg": "1.0mm²", "cpc_sizing_method": "bs7671_table_54.7", "cpc_sizing_clause": "BS 7671:2018+A2 Table 54.7 (adopted by KS 1700)", "zs_ohm": 1.45, "zs_max_ohm": 4.6,  "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_type": "A", "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C02", "db_designation": "MSP-100", "voltage_class": "LV_power", "breaker_rating_a": 6,  "breaker_curve": "B", "route_length_m": 22, "cpc_csa_mm2_or_awg": "1.0mm²", "cpc_sizing_method": "bs7671_table_54.7", "cpc_sizing_clause": "BS 7671:2018+A2 Table 54.7 (adopted by KS 1700)", "zs_ohm": 1.21, "zs_max_ohm": 7.67, "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_type": "A", "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C03", "db_designation": "MSP-100", "voltage_class": "LV_power", "breaker_rating_a": 32, "breaker_curve": "B", "route_length_m": 28, "cpc_csa_mm2_or_awg": "1.5mm²", "cpc_sizing_method": "bs7671_table_54.7", "cpc_sizing_clause": "BS 7671:2018+A2 Table 54.7 (adopted by KS 1700)", "zs_ohm": 1.34, "zs_max_ohm": 1.44, "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_type": "A", "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C04", "db_designation": "MSP-100", "voltage_class": "LV_power", "breaker_rating_a": 20, "breaker_curve": "B", "route_length_m": 18, "cpc_csa_mm2_or_awg": "2.5mm²", "cpc_sizing_method": "bs7671_table_54.7", "cpc_sizing_clause": "BS 7671:2018+A2 Table 54.7 (adopted by KS 1700)", "zs_ohm": 1.04, "zs_max_ohm": 2.3,  "zs_compliance": "pass_with_rcd", "rcd_required": true,  "rcd_type": "A", "rcd_sensitivity_ma": 30 },
    { "circuit_id": "C05", "db_designation": "MSP-100", "voltage_class": "LV_power", "breaker_rating_a": 20, "breaker_curve": "C", "route_length_m": 15, "cpc_csa_mm2_or_awg": "2.5mm²", "cpc_sizing_method": "iec60364_table_54.2", "cpc_sizing_clause": "IEC 60364-5-54:2011 Table 54.2", "zs_ohm": 0.99, "zs_max_ohm": 1.15, "zs_compliance": "pass", "rcd_required": false },
    { "circuit_id": "C06", "db_designation": "MSP-100", "voltage_class": "LV_power", "breaker_rating_a": 16, "breaker_curve": "C", "route_length_m": 12, "cpc_csa_mm2_or_awg": "1.5mm²", "cpc_sizing_method": "iec60364_table_54.2", "cpc_sizing_clause": "IEC 60364-5-54:2011 Table 54.2", "zs_ohm": 1.01, "zs_max_ohm": 1.44, "zs_compliance": "pass", "rcd_required": false },
    { "circuit_id": "C07", "db_designation": "MSP-100", "voltage_class": "LV_power", "breaker_rating_a": 25, "breaker_curve": "D", "route_length_m": 25, "cpc_csa_mm2_or_awg": "4mm²", "cpc_sizing_method": "iec60364_table_54.2", "cpc_sizing_clause": "IEC 60364-5-54:2011 Table 54.2", "zs_ohm": 0.99, "zs_max_ohm": 0.46, "zs_compliance": "fail_needs_rcd", "rcd_required": true,  "rcd_type": "A", "rcd_sensitivity_ma": 100 },
    { "circuit_id": "C08", "db_designation": "MSP-100", "voltage_class": "LV_power", "breaker_rating_a": 40, "breaker_curve": "B", "route_length_m": 60, "cpc_csa_mm2_or_awg": "6mm²", "cpc_sizing_method": "bs7671_table_54.7", "cpc_sizing_clause": "BS 7671:2018+A2 Table 54.7 (adopted by KS 1700)", "zs_ohm": 1.40, "zs_max_ohm": 1.15, "zs_compliance": "fail_needs_rcd", "rcd_required": true,  "rcd_type": "A", "rcd_sensitivity_ma": 30 }
  ],
  "compliance_summary": {
    "compliant": true,
    "non_compliance_flags": [
      { "message": "C07 compressor: Zs(0.99Ω) > Zs_max(0.46Ω) for Type D 25A — requires 100mA RCD to achieve disconnection time; without RCD this circuit would not meet 5s clearance.", "code_clause": "BS 7671:2018+A2 Reg 411.4.5 (adopted by KS 1700)", "severity": "warning" },
      { "message": "C08 60m submain: Zs(1.40Ω) > Zs_max(1.15Ω) for Type B 40A — requires 30mA RCBO to achieve 0.4s disconnection time on a final circuit ≤32A would normally apply, but as a submain feeding sockets in the gate house, RCD additional protection still required per KS 1700 §411.3.3.", "code_clause": "BS 7671:2018+A2 Reg 411.3.3 (adopted by KS 1700)", "severity": "warning" }
    ],
    "assumptions": [
      "Ze of 0.80Ω is KPLC-measured (2026-03-12); originally 0.65Ω at 1987 commissioning. Design ceiling assumes Ze ≤ 0.80Ω; supply degradation beyond this requires recalc.",
      "Cable impedance estimates use IEC 60364-5-52 Annex B values at 70°C PVC; XLPE not present in this design.",
      "KS 1700:2018 references BS 7671:2018+A2 verbatim for Table 41.2 Zs_max and Reg 411 disconnection times; cited as 'BS 7671 (adopted by KS 1700)' throughout.",
      "calc.zs_loop_impedance tool call pending — Zs values are LLM-estimates; deterministic tool will refine when runtime ships."
    ]
  },
  "drawn_as_symbols": ["EARTH_GENERAL", "EARTH_PROTECTIVE", "CONDUCTOR_PE", "JUNCTION_T", "BUSBAR", "MET_SYMBOL", "MCB_TYPE_D"],
  "flags": ["TOOL-CALL-PENDING"],
  "tool_call_pending_for_zs": true,
  "zs_calc_tool_input": {
    "jurisdiction": "KE",
    "supply": {
      "ze_declared_ohm": 0.80,
      "system_type": "TN-S",
      "voltage_phase_to_earth_v": 240,
      "frequency_hz": 50
    },
    "circuits": [
      { "circuit_id": "C01", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "1.5mm²", "cpc_csa_mm2_or_awg": "1.0mm²", "route_length_m": 35, "device_type": "MCB_B", "device_rating_a": 10 },
      { "circuit_id": "C02", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "1.5mm²", "cpc_csa_mm2_or_awg": "1.0mm²", "route_length_m": 22, "device_type": "MCB_B", "device_rating_a": 6  },
      { "circuit_id": "C03", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "2.5mm²", "cpc_csa_mm2_or_awg": "1.5mm²", "route_length_m": 28, "device_type": "MCB_B", "device_rating_a": 32 },
      { "circuit_id": "C04", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "2.5mm²", "cpc_csa_mm2_or_awg": "2.5mm²", "route_length_m": 18, "device_type": "MCB_B", "device_rating_a": 20 },
      { "circuit_id": "C05", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "2.5mm²", "cpc_csa_mm2_or_awg": "2.5mm²", "route_length_m": 15, "device_type": "MCB_C", "device_rating_a": 20 },
      { "circuit_id": "C06", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "1.5mm²", "cpc_csa_mm2_or_awg": "1.5mm²", "route_length_m": 12, "device_type": "MCB_C", "device_rating_a": 16 },
      { "circuit_id": "C07", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "4mm²",   "cpc_csa_mm2_or_awg": "4mm²",   "route_length_m": 25, "device_type": "MCB_D", "device_rating_a": 25 },
      { "circuit_id": "C08", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "10mm²",  "cpc_csa_mm2_or_awg": "6mm²",   "route_length_m": 60, "device_type": "MCB_B", "device_rating_a": 40 }
    ],
    "operating_temperature_c": 70
  },
  "rationale": {
    "chat_summary": "Nairobi light-engineering workshop on KPLC TN-S supply (Ze=0.80Ω). 8 circuits: workshop+office lighting and sockets, three 3-ph machine tools, 60m submain to gate house. C07 compressor (Type D 25A) needs 100mA RCD because the higher Type-D trip multiplier (20×In) demands lower Zs_max than the design Zs. C08 submain needs 30mA RCBO per KS 1700 §411.3.3. All other circuits Zs-compliant. calc.zs_loop_impedance tool deferred per WI3.",
    "sections": [
      {
        "title": "Earthing System",
        "summary": "TN-S confirmed from KPLC supply (separate-Earth cable, Ze=0.80Ω measured 2026-03-12). KS 1700:2018 §411.4 adopts BS 7671:2018+A2 Reg 411.4 verbatim for TN-S installations.",
        "decisions": [
          { "label": "TN-S system confirmed", "summary": "KPLC supply delivers a separate-Earth conductor from the 11kV/415V substation 180m away. No PME bond at intake.", "rule": "Ontology TN-S entry permitted for KE under KS 1700", "code_clause": "BS 7671:2018+A2 Reg 411.4 (adopted by KS 1700 §411.4)", "inputs": { "ze_ohm_declared": 0.80, "ze_provenance": "KPLC-measured 2026-03-12", "jurisdiction": "KE" } }
        ]
      },
      {
        "title": "Main Earthing Terminal",
        "summary": "MET in main switchroom adjacent to KPLC intake. Supply bond type 'tn_s_separate_pe' — Earth conductor terminated at the MET via the KPLC separate-Earth cable. Main earthing conductor 16mm² Cu.",
        "decisions": [
          { "label": "MET co-located with intake", "summary": "Accessible for inspection per BS 7671 542.4.1 (KS 1700 adopts).", "rule": "BS 7671 Reg 542.4.1 — MET accessible and labelled", "code_clause": "BS 7671:2018+A2 Reg 542.4.1", "inputs": { "met_location": "main switchroom adjacent to KPLC intake" } },
          { "label": "16mm² main earthing conductor", "summary": "Sized per Reg 544.1.1 from largest service conductor (25mm² → 16mm²); unprotected run between intake and MET ≥10mm². 16mm² satisfies both rules.", "rule": "BS 7671 Reg 544.1.1 sizing (KS 1700 adopts)", "code_clause": "BS 7671:2018+A2 Reg 544.1.1", "inputs": { "service_csa_mm2": 25, "result_csa_mm2": 16 } }
        ]
      },
      {
        "title": "Electrodes",
        "summary": "No supplementary electrode installed. KPLC TN-S supply provides the Earth reference. Recommendation: install a local rod electrode at the MET as a future-proofing measure against supply degradation (Ze rose from 0.65Ω to 0.80Ω over 39 years).",
        "decisions": [
          { "label": "No site electrode installed", "summary": "TN-S supply provides adequate Ze. Local electrode would not improve Zs significantly with Ze already declared.", "rule": "Engineering judgement — supply Ze adequate", "code_clause": "BS 7671:2018+A2 Reg 542.2 (electrodes optional on TN-S)", "inputs": { "ze_ohm": 0.80, "site_electrode_added": false } }
        ]
      },
      {
        "title": "Main Bonding",
        "summary": "Three main protective bonds from MET to all extraneous-conductive-parts: water service entry (10mm²), gas service entry (10mm²), structural steel columns (6mm²).",
        "decisions": [
          { "label": "10mm² Cu to water and gas services", "summary": "Service-entry bonds at TN-S minimum CSA per BS 7671 544.1.1.", "rule": "BS 7671 Reg 544.1.1 (KS 1700 adopts)", "code_clause": "BS 7671:2018+A2 Reg 544.1.1", "inputs": { "csa_mm2": 10, "targets": ["metal_water_service", "metal_gas_service"] } },
          { "label": "6mm² Cu to structural steel", "summary": "Half-of-largest-PE rule with 6mm² minimum applied.", "rule": "BS 7671 Reg 544.1.1", "code_clause": "BS 7671:2018+A2 Reg 544.1.1", "inputs": { "csa_mm2": 6, "target": "structural_steel" } }
        ]
      },
      {
        "title": "Supplementary Bonding",
        "summary": "Wash room with metal taps and partition — 4mm² supplementary bonding per BS 7671 Section 701 (KS 1700 §415).",
        "decisions": [
          { "label": "4mm² supplementary bonding wash room", "summary": "Belt-and-braces resilience for metallic plumbing + partition.", "rule": "BS 7671 Reg 415.2 (KS 1700 §415)", "code_clause": "BS 7671:2018+A2 Reg 415.2", "inputs": { "zone": "wash_room", "csa_mm2": 4 } }
        ]
      },
      {
        "title": "CPC Sizing",
        "summary": "Mixed CPC sizing methods. T+E twin-and-earth cables (C01-C04, C08) use BS 7671 Table 54.7 reduced-CPC values. SY 4-core cables (C05-C07) use IEC 60364-5-54 Table 54.2 separate-PE sizing.",
        "decisions": [
          { "label": "T+E CPCs from BS 7671 Table 54.7", "summary": "Manufacturer-supplied integral CPCs sized per banded lookup.", "rule": "BS 7671 Table 54.7", "code_clause": "BS 7671:2018+A2 Table 54.7", "inputs": { "circuits": ["C01", "C02", "C03", "C04", "C08"] } },
          { "label": "SY-cable CPCs from IEC 60364 Table 54.2", "summary": "Separate-PE conductor in flexible SY cable; sized at full CSA of line conductor.", "rule": "IEC 60364-5-54 Table 54.2 — PE = line CSA up to 16mm²", "code_clause": "IEC 60364-5-54:2011 Table 54.2", "inputs": { "circuits": ["C05", "C06", "C07"] } }
        ]
      },
      {
        "title": "Zs Verification (LLM-estimated; tool deferred)",
        "summary": "Zs = Ze + (R1+R2) computed inline per circuit. Zs_max from BS 7671 Table 41.2 (KS 1700 Annex E adopts verbatim). Verdict: 6 circuits pass; C07 (compressor Type D) fails Zs_max — needs 100mA RCD; C08 (60m submain) fails by 0.25Ω — needs 30mA RCBO.",
        "decisions": [
          { "label": "C07 compressor needs 100mA RCD", "summary": "Type D 25A demands Zs_max=0.46Ω (Ia=20×In). Calculated Zs=0.99Ω exceeds. RCD additional protection brings disconnection time within 5s.", "rule": "BS 7671 Reg 411.4.5 — RCD additional protection where Zs exceeds Zs_max", "code_clause": "BS 7671:2018+A2 Reg 411.4.5 (adopted by KS 1700)", "inputs": { "zs_ohm": 0.99, "zs_max_ohm": 0.46, "device_type": "MCB_D", "device_rating_a": 25, "rcd_sensitivity_ma": 100 } },
          { "label": "C08 submain needs 30mA RCBO", "summary": "60m run × 6mm² CPC pushes Zs to 1.40Ω; Type B 40A Zs_max is 1.15Ω. 30mA RCBO required.", "rule": "BS 7671 Reg 411.3.3 — RCD on final socket-feeding circuits + 411.4.5 ADS", "code_clause": "BS 7671:2018+A2 Reg 411.3.3 + Reg 411.4.5", "inputs": { "zs_ohm": 1.40, "zs_max_ohm": 1.15, "device_type": "MCB_B", "device_rating_a": 40, "rcd_sensitivity_ma": 30 } }
        ]
      },
      {
        "title": "RCD Policy (KE-specific)",
        "summary": "KS 1700 §411.3.3 mandates 30mA RCD on all socket circuits ≤32A regardless of Zs compliance (mirrors BS 7671 AMD 2 socket-RCD rule). Applied to C01-C04 + C08; not required for C05-C07 (no sockets, dedicated motor circuits).",
        "decisions": [
          { "label": "30mA RCD on all socket circuits", "summary": "KS 1700 mandates regardless of Zs result.", "rule": "KS 1700:2018 §411.3.3 (mirrors BS 7671 AMD 2)", "code_clause": "BS 7671:2018+A2 Reg 411.3.3 (adopted by KS 1700)", "inputs": { "circuits_rcd_protected": ["C01", "C02", "C03", "C04", "C08"], "rcd_sensitivity_ma": 30 } }
        ]
      },
      {
        "title": "Compliance",
        "summary": "Compliant via RCD additional protection on C07 + C08. 2 WARNING flags emitted (informational; both resolved by the RCD policy applied).",
        "decisions": []
      },
      {
        "title": "Tool Deferral (WI3)",
        "summary": "calc.zs_loop_impedance not yet runtime-shipped. Zs values above are LLM-estimates; the deterministic tool will refine when the runtime ships. zs_calc_tool_input replay payload captured at IR root for deterministic re-execution.",
        "decisions": [
          { "label": "Tool deferral flag set", "summary": "tool_call_pending_for_zs=true; rationale.flags contains TOOL-CALL-PENDING string.", "rule": "WI3 deferred-execution pattern", "code_clause": "n/a", "inputs": { "tool_call_pending_for_zs": true, "replay_payload_captured": true } }
        ]
      },
      {
        "title": "Assumptions",
        "summary": "Four assumptions captured: KPLC-measured Ze (2026-03-12), IEC 60364 Annex B cable impedance values used at 70°C PVC, KS 1700 adoption of BS 7671 Table 41.2, tool-call deferral per WI3.",
        "decisions": []
      }
    ]
  }
}
```

- [ ] **Step 2: Validate JSON syntax**

Run: `python3 -c "import json; json.load(open('electrical/earthing/examples/ke-nairobi-industrial-tn-s/output.json')); print('OK')"`
Expected: `OK`

- [ ] **Step 3: Validate against v1.1 schema**

Run:
```bash
python3 <<'PY'
import json, jsonschema
schema = json.load(open('electrical/earthing/schemas/earthing-ir.schema.json'))
data = json.load(open('electrical/earthing/examples/ke-nairobi-industrial-tn-s/output.json'))
jsonschema.validate(instance=data, schema=schema)
print('schema valid')
assert data['tool_call_pending_for_zs'] is True
assert 'zs_calc_tool_input' in data
assert data['jurisdiction'] == 'KE'
assert data['earthing_system']['system_type'] == 'TN-S'
assert len(data['circuits']) == 8
assert len(data['zs_calc_tool_input']['circuits']) == 8
print('content checks pass')
PY
```
Expected: `schema valid` then `content checks pass`.

- [ ] **Step 4: Commit**

```bash
git add electrical/earthing/examples/ke-nairobi-industrial-tn-s/output.json
git commit -m "$(cat <<'EOF'
feat(earthing): KE Nairobi example — output.json with WI3 tool deferral

Full IR for the Nairobi light-engineering workshop. 8 circuits;
C07 compressor (Type D 25A) and C08 60m submain both require RCD
additional protection. tool_call_pending_for_zs=true with full
zs_calc_tool_input replay payload at IR root. KS 1700 references
cited as 'adopted by KS 1700' alongside BS 7671 sections.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9 — Create KE example reasoning.md

**Files:**
- Create: `electrical/earthing/examples/ke-nairobi-industrial-tn-s/reasoning.md`

- [ ] **Step 1: Write reasoning.md**

````markdown
# Reasoning — KE Nairobi Industrial TN-S

## Site context

Light engineering workshop on Enterprise Road, Nairobi Industrial Area. 1980s build, KPLC supply via dedicated 11kV/415V substation 180m down the road. Three-phase 415V/240V 4-wire 50 Hz with 100A TPN main switch.

Supply is **TN-S** — separate-Earth cable from substation transformer star point, terminated at the MET. KPLC declared Ze = 0.65Ω at 1987 commissioning; the latest measurement in 2026 shows Ze = 0.80Ω. The slow rise over 39 years is typical of older TN-S cables with progressive sheath corrosion. Design ceiling assumes 0.80Ω; further degradation triggers recalc.

## Why TN-S (not TN-C-S)

KPLC supplies a small number of legacy industrial sites on TN-S because the substation's neutral-earth bond is at the transformer star point only — there is no PEN conductor combining N and Earth at the consumer's intake. Most modern KPLC installations are TN-C-S (PME) with consumer-side N/E split, but older industrial sites in Industrial Area retain TN-S because the supply infrastructure predates PME rollout.

TN-S vs TN-C-S engineering differences exercised by this example:
- **Higher Ze.** TN-S typically 0.65-1.0Ω; TN-C-S typically 0.20-0.35Ω. This squeezes the Zs compliance margin on every circuit.
- **No PEN at intake.** Supply bond type is `tn_s_separate_pe` rather than `dno_pme_bond`. Affects the schematic symbology + supplementary bonding obligations.
- **DNO declaration uncertainty.** TN-S Ze degrades over building life; engineers should verify by measurement rather than relying on the original declaration alone.

## Why the compressor needs a 100mA RCD

C07 is a 3-phase 7.5kW reciprocating compressor with start-stop duty cycle. The high inrush demands a **Type D MCB** (Ia = 20 × In) to ride out the starting transient without nuisance tripping. The downside: Type D's higher trip multiplier requires a **lower Zs_max** to ensure the device trips in time on an earth fault:

```
For MCB_D at 25A:
  Zs_max = 230 / (20 × 25) = 0.46 Ω
```

This circuit's calculated Zs = 0.99Ω, which exceeds 0.46Ω. Without additional protection, the MCB would not trip within 5 seconds — non-compliant with BS 7671 Reg 411.4.5 (adopted by KS 1700).

The standard fix per BS 7671 Reg 411.4.5 is to add **RCD additional protection**. The 100mA RCD (rather than 30mA) is selected because:
- The compressor has a high earth leakage current at start-up (motor inrush + capacitive coupling in the SY cable)
- 30mA RCDs nuisance-trip on this duty profile
- 100mA RCD provides shock protection at fault levels without nuisance tripping during normal duty

## Why the gate-house submain needs a 30mA RCBO

C08 is a 60m run to a gate house, terminating in a small 4-way sub-DB serving sockets and lighting. The long run with 6mm² CPC pushes Zs above Type B 40A's Zs_max:

```
For MCB_B at 40A:
  Zs_max = 230 / (5 × 40) = 1.15 Ω
Calculated Zs = Ze + (R1+R2)
             ≈ 0.80 + 0.60
             ≈ 1.40 Ω
```

This exceeds 1.15Ω. Two compliance paths:

1. **Upsize the CPC to 10mm²** — R2 drops, Zs drops below 1.15Ω. Costly cable replacement.
2. **Add 30mA RCBO** — RCD additional protection achieves 0.4s disconnection on socket-final-circuit fault. Faster and cheaper.

We chose option 2. The 30mA sensitivity is required by KS 1700 §411.3.3 anyway (socket circuits ≤32A — even though this is a 40A submain, the downstream sockets behind it inherit the requirement).

## Kenya-specific RCD policy

KS 1700:2018 §411.3.3 mandates 30mA RCD on all socket circuits regardless of Zs compliance. This mirrors the BS 7671 AMD 2 (2022) socket-RCD rule but with one key difference: KS 1700 applies it from initial adoption, while BS 7671's rule was retrofit. Practically, Kenyan installations have been wired this way since 2018; UK engineers reviewing a Kenyan design should expect blanket RCD coverage on all socket circuits as standard practice.

This is why C01-C04 + C08 are all 30mA RCD-protected even when Zs alone wouldn't demand it. C05-C07 are dedicated machine circuits (no sockets) and are not RCD-protected as a matter of course.

## KS 1700 vs BS 7671

KS 1700:2018 references BS 7671 extensively. The substantive sections (411 ADS, 415 supplementary bonding, 544 main bonding, Table 41.2 Zs_max, Table 54.7 CPC sizing) are adopted **verbatim** via KS 1700 Annex E. Where KS 1700 diverges from BS 7671:
- §411.3.3 (socket-RCD) — adopted from BS 7671 AMD 2 ahead of UK retrofit
- §722 (EV charging) — not yet in KS 1700; falls back to IEC 60364-7-722
- §731-740 (special locations) — partial adoption; verify per-section before applying

Our citations use the form `"BS 7671:2018+A2 Reg X.Y.Z (adopted by KS 1700)"` to make the adoption explicit.

## Why the Zs values are LLM-estimates

`calc.zs_loop_impedance` is the new WI3-deferred tool contract introduced in this skill version (v1.1.0). The deterministic Python implementation lives in the DraftsMan runtime — it has not yet shipped. Until then, the LLM produces rough Zs estimates inline using approximate per-CSA cable resistance values.

The `zs_calc_tool_input` block at the IR root captures the complete payload the tool will consume when it ships. The runtime can replay this against `IEC60364/part5-52-cable-impedance.json` (the canonical per-CSA AC R+X table shipped 2026-05-17) without re-prompting the LLM.

Expect refinement of ±5-15% on Zs values when the tool ships. The compliance verdicts (pass / pass_with_rcd / fail_needs_rcd) should not change for this example.

## Tool deferral check

When `tool_call_pending_for_zs: true`:
- `zs_calc_tool_input` is present and well-formed (verified by validator INV-9)
- `rationale.flags` contains `TOOL-CALL-PENDING:calc.zs_loop_impedance` disclaimer

When the runtime tool ships, both go away — `tool_call_pending_for_zs: false`, the flag is removed, and `circuits[].zs_ohm` carries tool-precision values.
````

- [ ] **Step 2: Validate file written**

Run: `wc -l electrical/earthing/examples/ke-nairobi-industrial-tn-s/reasoning.md`
Expected: ~80+ lines.

- [ ] **Step 3: Commit**

```bash
git add electrical/earthing/examples/ke-nairobi-industrial-tn-s/reasoning.md
git commit -m "$(cat <<'EOF'
feat(earthing): KE Nairobi example — reasoning.md

Engineer narrative for the Nairobi industrial TN-S example. Covers
TN-S vs TN-C-S split, Type D MCB Zs squeeze, 60m submain RCBO
requirement, KE-specific RCD policy per KS 1700 §411.3.3, KS 1700
vs BS 7671 adoption pattern, and WI3 tool deferral context.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10 — Create KE example sample-schedule.md

**Files:**
- Create: `electrical/earthing/examples/ke-nairobi-industrial-tn-s/sample-schedule.md`

- [ ] **Step 1: Write sample-schedule.md**

```markdown
# Zs Verification Schedule — Nairobi Industrial TN-S

**Project:** ke-nairobi-enterprise-light-engineering
**Generated:** 2026-05-18
**Jurisdiction:** KE (KS 1700:2018 + BS 7671:2018+A2)
**Supply:** KPLC TN-S 415V/240V 50Hz, Ze = 0.80 Ω
**Standards:** BS 7671 Table 41.2 (adopted by KS 1700 Annex E) for Zs_max; IEC 60364-5-52 Annex B for cable R+X
**Tool status:** `calc.zs_loop_impedance` deferred per WI3 — values below are LLM-estimates

---

| Circuit | Ib (A) | Device | In (A) | Cable (line/CPC) | Length (m) | R1+R2 (Ω) | Zs_calc (Ω) | Zs_max (Ω) | Margin (%) | Verdict | RCD |
|---|---|---|---|---|---|---|---|---|---|---|---|
| C01 | 7.5  | MCB_B  | 10 | 1.5mm² / 1.0mm² | 35 | 0.65 | 1.45 | 4.60 | +68.5% | pass | 30mA |
| C02 | 4.5  | MCB_B  | 6  | 1.5mm² / 1.0mm² | 22 | 0.41 | 1.21 | 7.67 | +84.2% | pass | 30mA |
| C03 | 28.0 | MCB_B  | 32 | 2.5mm² / 1.5mm² | 28 | 0.54 | 1.34 | 1.44 | +6.9%  | pass | 30mA |
| C04 | 16.0 | MCB_B  | 20 | 2.5mm² / 2.5mm² | 18 | 0.24 | 1.04 | 2.30 | +54.8% | pass | 30mA |
| C05 | 9.6  | MCB_C  | 20 | 2.5mm² / 2.5mm² | 15 | 0.19 | 0.99 | 1.15 | +13.9% | pass | n/a  |
| C06 | 4.2  | MCB_C  | 16 | 1.5mm² / 1.5mm² | 12 | 0.21 | 1.01 | 1.44 | +29.9% | pass | n/a  |
| C07 | 14.0 | MCB_D  | 25 | 4mm² / 4mm²     | 25 | 0.19 | 0.99 | 0.46 | -53.5% | **fail_needs_rcd** | 100mA |
| C08 | 35.0 | MCB_B  | 40 | 10mm² / 6mm² SWA| 60 | 0.60 | 1.40 | 1.15 | -17.9% | **fail_needs_rcd** | 30mA  |

---

## Notes

- **Ib (design current)** is at full design load. Actual operating current will be lower under partial load.
- **R1+R2** is the loop resistance of line + CPC at 70°C PVC operating temperature, per IEC 60364-5-52 Annex B.
- **Zs_calc** = Ze (0.80 Ω) + R1+R2.
- **Zs_max** from BS 7671 Table 41.2 (KS 1700 Annex E adopts verbatim). Table values already include the 0.8 temperature correction.
- **Margin** = (Zs_max − Zs_calc) / Zs_max × 100. Positive = pass; negative = fail.
- **Verdict** key:
  - `pass` — Zs ≤ Zs_max; protective device alone provides disconnection
  - `pass_with_rcd` — Zs ≤ Zs_max AND KS 1700 §411.3.3 mandates RCD on this socket circuit
  - `fail_needs_rcd` — Zs > Zs_max; RCD required to achieve disconnection time
  - `fail_oversize_cpc` — Zs > Zs_max even with RCD threshold; CPC must be oversized

## Failures explained

- **C07 (Compressor 7.5kW)**: Type D MCB demands Ia = 20×In, giving Zs_max = 230/(20×25) = 0.46 Ω. Calculated Zs (0.99 Ω) exceeds. 100mA RCD specified (higher than 30mA to avoid nuisance trips on motor leakage).
- **C08 (60m submain)**: Long route × 6mm² CPC pushes Zs to 1.40 Ω vs Type B 40A Zs_max of 1.15 Ω. 30mA RCBO required — also satisfies KS 1700 §411.3.3 socket-circuit RCD policy at the gate-house downstream DB.

## Verification

Once `calc.zs_loop_impedance` runtime tool ships, the engineer will rerun this schedule deterministically. Expected refinement: ±5-15% on Zs values; verdicts should not change for this example.
```

- [ ] **Step 2: Validate file written**

Run: `grep -c "^|" electrical/earthing/examples/ke-nairobi-industrial-tn-s/sample-schedule.md`
Expected: 10 or more (8 data rows + header + separator).

- [ ] **Step 3: Commit**

```bash
git add electrical/earthing/examples/ke-nairobi-industrial-tn-s/sample-schedule.md
git commit -m "$(cat <<'EOF'
feat(earthing): KE Nairobi example — sample-schedule.md

Engineer-readable Zs verification schedule, 8-row markdown table.
Shows Ib / In / device / cable / length / R1+R2 / Zs_calc / Zs_max
/ margin / verdict / RCD per circuit. Explains the 2 failing
circuits + tool-deferral notice.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11 — Retrofit UK TN-C-S example

**Files:**
- Modify: `electrical/earthing/examples/uk-dwelling-tn-cs/output.json`
- Modify: `electrical/earthing/examples/uk-dwelling-tn-cs/reasoning.md`

- [ ] **Step 1: Read current output.json to confirm structure**

Use the Read tool on `electrical/earthing/examples/uk-dwelling-tn-cs/output.json`. Locate the `flags` field and the closing `rationale.chat_summary` field — these are the insertion points.

- [ ] **Step 2: Add tool_call_pending_for_zs + zs_calc_tool_input + TOOL-CALL-PENDING flag**

Use the Edit tool. Replace exactly:

```json
  "drawn_as_symbols": ["EARTH_GENERAL", "EARTH_PROTECTIVE", "CONDUCTOR_PE", "CONDUCTOR_PEN", "JUNCTION_T", "BUSBAR"],
  "flags": [],
  "rationale": {
    "chat_summary": "TN-C-S earthing for a 3-bed UK semi on PME supply (Ze=0.35Ω). MET in hallway cupboard with 10mm² bonds to water and gas and 6mm² to loft structural steel. Six final circuits, all with CPCs sized per BS 7671 Table 54.7 and protected by the 30mA main RCD. Bathroom has supplementary 4mm² bonding. All circuits Zs-compliant with margin. Reply to refine, e.g. 'add solar PV'.",
```

With:

```json
  "drawn_as_symbols": ["EARTH_GENERAL", "EARTH_PROTECTIVE", "CONDUCTOR_PE", "CONDUCTOR_PEN", "JUNCTION_T", "BUSBAR"],
  "flags": ["TOOL-CALL-PENDING"],
  "tool_call_pending_for_zs": true,
  "zs_calc_tool_input": {
    "jurisdiction": "GB",
    "supply": {
      "ze_declared_ohm": 0.35,
      "system_type": "TN-C-S",
      "voltage_phase_to_earth_v": 230,
      "frequency_hz": 50
    },
    "circuits": [
      { "circuit_id": "C01", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "1.5mm²", "cpc_csa_mm2_or_awg": "1.0mm²", "route_length_m": 18, "device_type": "MCB_B", "device_rating_a": 6  },
      { "circuit_id": "C02", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "1.5mm²", "cpc_csa_mm2_or_awg": "1.0mm²", "route_length_m": 22, "device_type": "MCB_B", "device_rating_a": 6  },
      { "circuit_id": "C03", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "2.5mm²", "cpc_csa_mm2_or_awg": "1.5mm²", "route_length_m": 38, "device_type": "MCB_B", "device_rating_a": 32 },
      { "circuit_id": "C04", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "2.5mm²", "cpc_csa_mm2_or_awg": "1.5mm²", "route_length_m": 42, "device_type": "MCB_B", "device_rating_a": 32 },
      { "circuit_id": "C05", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "2.5mm²", "cpc_csa_mm2_or_awg": "2.5mm²", "route_length_m": 14, "device_type": "MCB_B", "device_rating_a": 32 },
      { "circuit_id": "C06", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "4mm²",   "cpc_csa_mm2_or_awg": "4mm²",   "route_length_m": 16, "device_type": "MCB_B", "device_rating_a": 40 }
    ],
    "operating_temperature_c": 70
  },
  "rationale": {
    "chat_summary": "TN-C-S earthing for a 3-bed UK semi on PME supply (Ze=0.35Ω). MET in hallway cupboard with 10mm² bonds to water and gas and 6mm² to loft structural steel. Six final circuits, all with CPCs sized per BS 7671 Table 54.7 and protected by the 30mA main RCD. Bathroom has supplementary 4mm² bonding. All circuits Zs-compliant with margin. v1.1 retrofit: calc.zs_loop_impedance tool deferred per WI3.",
```

- [ ] **Step 3: Append TOOL-CALL-PENDING string to rationale.flags within rationale**

The rationale object has a separate `flags` field (different from the top-level `flags`). Locate it. Wait — looking at the rationale schema, the rationale has `sections` and `chat_summary`, not `flags`. Re-check: the top-level `flags` is where TOOL-CALL-PENDING goes. Looking at the schema, `rationale` does NOT have a flags subfield — top-level `flags` array carries it.

Wait, but the validator YAML in Task 4 says `ir.rationale.flags must include string matching...`. Let me reconcile.

**Decision:** the TOOL-CALL-PENDING string lives in the top-level `flags` array (not `rationale.flags` — there is no such field per schema). Update the validator YAML in Task 4 to reference `ir.flags` instead. The implementer in Task 4 should adjust this.

But since this plan is sequential, by Task 11 the validator YAML is already committed. Issue a correction:

Use the Edit tool on `electrical/earthing/validation/tool-deferral-shape.yaml`:
- Replace every occurrence of `ir.rationale.flags` with `ir.flags`
- Re-commit as: `fix(earthing): tool-deferral-shape validator references top-level ir.flags, not ir.rationale.flags`

Now back to Task 11. The current top-level `flags: []` is being replaced with `flags: ["TOOL-CALL-PENDING"]`. The full TOOL-CALL-PENDING string per spec §5 is:

`"TOOL-CALL-PENDING:calc.zs_loop_impedance — Zs values are LLM-estimates; deterministic refinement deferred per WI3."`

That's longer than just `"TOOL-CALL-PENDING"`. **Use the full string** in the flags array. Re-do step 2 above with:

```json
  "flags": ["TOOL-CALL-PENDING:calc.zs_loop_impedance — Zs values are LLM-estimates; deterministic refinement deferred per WI3."],
```

Re-verify the change.

- [ ] **Step 4: Validate against v1.1 schema**

Run:
```bash
python3 <<'PY'
import json, jsonschema
schema = json.load(open('electrical/earthing/schemas/earthing-ir.schema.json'))
data = json.load(open('electrical/earthing/examples/uk-dwelling-tn-cs/output.json'))
jsonschema.validate(instance=data, schema=schema)
assert data['tool_call_pending_for_zs'] is True
assert any('TOOL-CALL-PENDING:calc.zs_loop_impedance' in f for f in data['flags'])
assert 'zs_calc_tool_input' in data
print('OK')
PY
```
Expected: `OK`

- [ ] **Step 5: Add retrofit note to reasoning.md**

Use the Edit tool to find the first heading in `electrical/earthing/examples/uk-dwelling-tn-cs/reasoning.md` (likely `# Reasoning ...`). Insert a new paragraph IMMEDIATELY after the H1 heading:

```markdown
> **v1.1 retrofit (2026-05-17):** This example now declares `calc.zs_loop_impedance` tool deferral per WI3. The Zs values below are unchanged from v1.0.0 (LLM-computed inline); the deterministic tool will refine them when the DraftsMan runtime ships. The `zs_calc_tool_input` replay payload was added at the IR root so the tool can re-execute deterministically.
```

- [ ] **Step 6: Commit**

```bash
git add electrical/earthing/examples/uk-dwelling-tn-cs/output.json electrical/earthing/examples/uk-dwelling-tn-cs/reasoning.md electrical/earthing/validation/tool-deferral-shape.yaml
git commit -m "$(cat <<'EOF'
fix(earthing): UK TN-C-S example retrofit + validator path correction

Adds tool_call_pending_for_zs=true + zs_calc_tool_input replay payload
+ TOOL-CALL-PENDING:calc.zs_loop_impedance string in top-level flags.
Zs values themselves unchanged from v1.0.0.

Corrects tool-deferral-shape validator to reference ir.flags rather
than ir.rationale.flags (the schema has no rationale.flags field).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 12 — Retrofit INT TT example

**Files:**
- Modify: `electrical/earthing/examples/intl-rural-tt/output.json`
- Modify: `electrical/earthing/examples/intl-rural-tt/reasoning.md`

- [ ] **Step 1: Read current output.json**

Read `electrical/earthing/examples/intl-rural-tt/output.json`. Note the circuits[] data — typically 3 circuits per the earlier inspection.

- [ ] **Step 2: Add the retrofit fields**

Use the Edit tool. Locate the line containing `"flags": [...]` near the end of the file (or `"flags": []` if empty). Replace it with:

```json
  "flags": ["TOOL-CALL-PENDING:calc.zs_loop_impedance — Zs values are LLM-estimates; deterministic refinement deferred per WI3."],
  "tool_call_pending_for_zs": true,
  "zs_calc_tool_input": {
    "jurisdiction": "INT",
    "supply": {
      "ze_declared_ohm": 1000.0,
      "system_type": "TT",
      "voltage_phase_to_earth_v": 230,
      "frequency_hz": 50
    },
    "circuits": [
      { "circuit_id": "C01", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "1.5mm²", "cpc_csa_mm2_or_awg": "1.5mm²", "route_length_m": 25, "device_type": "MCB_B", "device_rating_a": 10 },
      { "circuit_id": "C02", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "2.5mm²", "cpc_csa_mm2_or_awg": "2.5mm²", "route_length_m": 28, "device_type": "MCB_C", "device_rating_a": 16 },
      { "circuit_id": "C03", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "4mm²",   "cpc_csa_mm2_or_awg": "4mm²",   "route_length_m": 35, "device_type": "MCB_C", "device_rating_a": 20 }
    ],
    "operating_temperature_c": 70
  },
```

(Notes: `ze_declared_ohm: 1000.0` is illustrative — TT systems have very high Ze that's not declared in the same way as TN. Cross-check with the existing example to confirm the value used; the existing example's `zs_ohm ≈ 85` and `zs_max_ohm ≈ 1667` suggest Ra-electrode rather than Ze-declared. If the example uses a different convention, use the value that's actually in the existing data.)

**Action:** Read the existing `output.json` and locate the value used. The TT system's "Ze" in this example is the SUM of the source impedance + electrode resistance (Ra). Use whatever value is consistent with the existing circuit Zs values. If unclear, set `ze_declared_ohm` to match the existing `zs_ohm` minus the per-circuit R1+R2 contribution — likely around 85 Ω.

- [ ] **Step 3: Validate against v1.1 schema**

Run the same `python3 jsonschema.validate` block as Task 11 Step 4, substituting the path.
Expected: `OK`

- [ ] **Step 4: Add retrofit note to reasoning.md**

Use the Edit tool. After the first H1 heading in `electrical/earthing/examples/intl-rural-tt/reasoning.md`, insert:

```markdown
> **v1.1 retrofit (2026-05-17):** This example now declares `calc.zs_loop_impedance` tool deferral per WI3. The Zs values below are unchanged from v1.0.0 (LLM-computed inline); the deterministic tool will refine them when the DraftsMan runtime ships. The `zs_calc_tool_input` replay payload was added at the IR root so the tool can re-execute deterministically.
```

- [ ] **Step 5: Commit**

```bash
git add electrical/earthing/examples/intl-rural-tt/output.json electrical/earthing/examples/intl-rural-tt/reasoning.md
git commit -m "$(cat <<'EOF'
fix(earthing): INT TT example retrofit — tool deferral fields

Adds tool_call_pending_for_zs=true + zs_calc_tool_input replay payload
+ TOOL-CALL-PENDING string in flags. Zs values unchanged.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 13 — Retrofit US NEC example

**Files:**
- Modify: `electrical/earthing/examples/us-commercial-nec/output.json`
- Modify: `electrical/earthing/examples/us-commercial-nec/reasoning.md`

- [ ] **Step 1: Read current output.json**

Read the file. Note the circuits[] data and existing Ze/Zs values.

- [ ] **Step 2: Add the retrofit fields**

Use the Edit tool. Locate `"flags": [...]` near the end. Replace with:

```json
  "flags": ["TOOL-CALL-PENDING:calc.zs_loop_impedance — Zs values are LLM-estimates; deterministic refinement deferred per WI3."],
  "tool_call_pending_for_zs": true,
  "zs_calc_tool_input": {
    "jurisdiction": "US",
    "supply": {
      "ze_declared_ohm": 0.10,
      "system_type": "TN-S",
      "voltage_phase_to_earth_v": 277,
      "frequency_hz": 60
    },
    "circuits": [
      { "circuit_id": "C01", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "12 AWG", "cpc_csa_mm2_or_awg": "12 AWG", "route_length_m": 25, "device_type": "MCB_C", "device_rating_a": 20 },
      { "circuit_id": "C02", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "12 AWG", "cpc_csa_mm2_or_awg": "12 AWG", "route_length_m": 30, "device_type": "MCB_C", "device_rating_a": 20 },
      { "circuit_id": "C03", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "10 AWG", "cpc_csa_mm2_or_awg": "10 AWG", "route_length_m": 18, "device_type": "MCB_C", "device_rating_a": 30 },
      { "circuit_id": "C04", "cable_material": "Cu", "cable_insulation": "PVC", "line_csa_mm2_or_awg": "6 AWG",  "cpc_csa_mm2_or_awg": "10 AWG", "route_length_m": 35, "device_type": "MCB_C", "device_rating_a": 60 }
    ],
    "operating_temperature_c": 75
  },
```

(Note: `ze_declared_ohm: 0.10` is typical for US commercial — strong utility supply. Adjust if the existing example specifies a different value. `operating_temperature_c: 75` matches NEC 75°C ampacity table convention.)

- [ ] **Step 3: Validate**

Same jsonschema check as Tasks 11/12.

- [ ] **Step 4: Add retrofit note to reasoning.md**

Insert after the first H1:

```markdown
> **v1.1 retrofit (2026-05-17):** This example now declares `calc.zs_loop_impedance` tool deferral per WI3. The Zs values below are unchanged from v1.0.0 (LLM-computed inline); the deterministic tool will refine them when the DraftsMan runtime ships. The `zs_calc_tool_input` replay payload was added at the IR root so the tool can re-execute deterministically.
```

- [ ] **Step 5: Commit**

```bash
git add electrical/earthing/examples/us-commercial-nec/output.json electrical/earthing/examples/us-commercial-nec/reasoning.md
git commit -m "$(cat <<'EOF'
fix(earthing): US NEC example retrofit — tool deferral fields

Adds tool_call_pending_for_zs=true + zs_calc_tool_input replay payload
(US jurisdiction routes through NFPA70 Chapter 9 cable impedance +
NEC trip-current derivation) + TOOL-CALL-PENDING string in flags.
Zs values unchanged.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 14 — Create eval-07-ke-tn-s-tool-deferral.yaml

**Files:**
- Create: `electrical/earthing/evals/eval-07-ke-tn-s-tool-deferral.yaml`

- [ ] **Step 1: Read the eval schema + an existing eval for reference**

Run:
```bash
cat shared/schemas/core/eval.schema.json | head -50
cat electrical/earthing/evals/eval-06-rationale-block.yaml
```

Note the assertion grammar (jsonpath + operator + value).

- [ ] **Step 2: Write eval-07**

```yaml
name: eval-07-ke-tn-s-tool-deferral
skill: earthing
description: Verifies the Kenya TN-S worked example output. Exercises TN-S system_type branch, KE jurisdiction tag, WI3 tool deferral pattern (tool_call_pending_for_zs + zs_calc_tool_input + TOOL-CALL-PENDING flag string), Type D MCB Zs failure path, 60m submain RCD requirement, and KS 1700 RCD policy on socket circuits.
category: skill_specific
input_fixture: ../examples/ke-nairobi-industrial-tn-s/input.json
expected_output_fixture: ../examples/ke-nairobi-industrial-tn-s/output.json
checks:
  - assertion: ir.jurisdiction == "KE"
    description: "Jurisdiction tag is KE (Kenya)"
    severity: critical
  - assertion: ir.earthing_system.system_type == "TN-S"
    description: "TN-S system branch exercised"
    severity: critical
  - assertion: ir.met.supply_bond_type == "tn_s_separate_pe"
    description: "TN-S separate-Earth supply bond used (not PME)"
    severity: critical
  - assertion: ir.tool_call_pending_for_zs == true
    description: "WI3 tool deferral flag set"
    severity: critical
  - assertion: ir.zs_calc_tool_input.jurisdiction == "KE"
    description: "Replay payload tagged with KE jurisdiction"
    severity: critical
  - assertion: ir.zs_calc_tool_input.supply.system_type == "TN-S"
    description: "Replay payload supply is TN-S"
    severity: critical
  - assertion: ir.zs_calc_tool_input.supply.ze_declared_ohm == 0.80
    description: "Replay payload Ze matches KPLC-measured value"
    severity: critical
  - assertion: ir.zs_calc_tool_input.circuits.length == 8
    description: "All 8 circuits captured in replay payload"
    severity: critical
  - assertion: ir.flags matches "^TOOL-CALL-PENDING:calc\\.zs_loop_impedance"
    description: "TOOL-CALL-PENDING:calc.zs_loop_impedance disclaimer present in top-level flags"
    severity: critical
  - assertion: ir.circuits[?(@.circuit_id=='C07')].zs_compliance == "fail_needs_rcd"
    description: "C07 compressor (Type D 25A) fails Zs_max — needs RCD additional protection"
    severity: critical
  - assertion: ir.circuits[?(@.circuit_id=='C07')].rcd_sensitivity_ma == 100
    description: "C07 uses 100mA RCD (motor-leakage-tolerant)"
    severity: warning
  - assertion: ir.circuits[?(@.circuit_id=='C08')].zs_compliance == "fail_needs_rcd"
    description: "C08 60m submain fails Zs_max"
    severity: critical
  - assertion: ir.circuits[?(@.circuit_id=='C08')].rcd_sensitivity_ma == 30
    description: "C08 uses 30mA RCBO per KS 1700 §411.3.3 socket-circuit policy"
    severity: critical
  - assertion: ir.compliance_summary.compliant == true
    description: "Overall compliant via RCD additional protection"
    severity: critical
  - assertion: ir.drawn_as_symbols contains "EARTH_PROTECTIVE"
    description: "Earthing symbol roll-up includes EARTH_PROTECTIVE"
    severity: warning
expected_flags:
  - "TOOL-CALL-PENDING:calc.zs_loop_impedance"
```

- [ ] **Step 3: Validate YAML**

Run: `python3 -c "import yaml; yaml.safe_load(open('electrical/earthing/evals/eval-07-ke-tn-s-tool-deferral.yaml')); print('OK')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add electrical/earthing/evals/eval-07-ke-tn-s-tool-deferral.yaml
git commit -m "$(cat <<'EOF'
feat(earthing): eval-07 — KE TN-S worked path + WI3 tool deferral

15 assertions verifying:
- KE jurisdiction tag + TN-S system branch + separate-PE supply bond
- tool_call_pending_for_zs flag + zs_calc_tool_input replay payload
- TOOL-CALL-PENDING string in rationale flags
- C07 Type D 25A compressor Zs failure → 100mA RCD
- C08 60m submain Zs failure → 30mA RCBO (KS 1700 §411.3.3)
- Overall compliance via RCD additional protection
- Drawing symbol roll-up

Category: skill_specific (skill-specific WI3 + KE jurisdiction proof).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 15 — Update skill.manifest.json

**Files:**
- Modify: `electrical/earthing/skill.manifest.json`

- [ ] **Step 1: Bump version + add new file references**

Use the Edit tool to make these specific changes:

Replace:
```json
  "version": "1.0.0",
```
With:
```json
  "version": "1.1.0",
```

In the `calculations` array, replace:
```json
  "calculations": [
    "shared/calculations/electrical/electrode-resistance.json",
    "shared/calculations/electrical/cpc-adiabatic.json"
  ],
```
With:
```json
  "calculations": [
    "shared/calculations/electrical/electrode-resistance.json",
    "shared/calculations/electrical/cpc-adiabatic.json",
    "shared/calculations/electrical/zs-loop-impedance.json"
  ],
```

In the validators array (locate it after the constraints/rules arrays), add the new YAML. Use Edit to find the existing validators array (it includes constraint YAMLs from validation/ subfolder) and append `electrical/earthing/validation/tool-deferral-shape.yaml`.

In the evals array, replace it with the 7-entry version including eval-07:
```json
  "evals": [
    "electrical/earthing/evals/eval-01-uk-dwelling-tn-cs.yaml",
    "electrical/earthing/evals/eval-02-rural-tt-system.yaml",
    "electrical/earthing/evals/eval-03-cpc-undersized-trap.yaml",
    "electrical/earthing/evals/eval-04-missing-soil-resistivity.yaml",
    "electrical/earthing/evals/eval-05-jurisdiction-us-nec.yaml",
    "electrical/earthing/evals/eval-06-rationale-block.yaml",
    "electrical/earthing/evals/eval-07-ke-tn-s-tool-deferral.yaml"
  ],
```

In the examples array, add the new KE example:
```json
  "examples": [
    "electrical/earthing/examples/uk-dwelling-tn-cs/",
    "electrical/earthing/examples/intl-rural-tt/",
    "electrical/earthing/examples/us-commercial-nec/",
    "electrical/earthing/examples/ke-nairobi-industrial-tn-s/"
  ],
```

- [ ] **Step 2: Validate JSON**

Run: `python3 -c "import json; d=json.load(open('electrical/earthing/skill.manifest.json')); assert d['version']=='1.1.0'; assert any('zs-loop-impedance' in c for c in d['calculations']); assert any('ke-nairobi' in e for e in d['examples']); assert any('eval-07' in e for e in d['evals']); print('OK')"`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add electrical/earthing/skill.manifest.json
git commit -m "$(cat <<'EOF'
chore(earthing): manifest v1.1.0 — register new calc + example + eval + validator

Version 1.0.0 → 1.1.0. Adds:
- shared/calculations/electrical/zs-loop-impedance.json
- electrical/earthing/validation/tool-deferral-shape.yaml
- electrical/earthing/examples/ke-nairobi-industrial-tn-s/
- electrical/earthing/evals/eval-07-ke-tn-s-tool-deferral.yaml

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 16 — Update CHANGELOG.md

**Files:**
- Modify: `electrical/earthing/CHANGELOG.md`

- [ ] **Step 1: Read current CHANGELOG**

Run: `head -30 electrical/earthing/CHANGELOG.md` to see the format.

- [ ] **Step 2: Insert v1.1.0 entry at the top**

Use the Edit tool to find the existing top-most version entry (likely `## [1.0.0]` or similar). Insert IMMEDIATELY BEFORE it:

```markdown
## [1.1.0] - 2026-05-17

### Added
- **`calc.zs_loop_impedance` calc contract** (`shared/calculations/electrical/zs-loop-impedance.json`) — WI3-deferred tool for deterministic Zs verification. Jurisdiction-branching: GB/KE/EU/INT use IEC 60364-5-52 Annex B + BS 7671 Table 41.2; US uses NFPA70 Chapter 9 + NEC 5×In trip-current derivation.
- **`tool_call_pending_for_zs` + `zs_calc_tool_input`** root fields in `earthing-ir.schema.json` — replay payload captured at generation time for deterministic tool execution.
- **`KE` jurisdiction enum value** — Kenya (KS 1700:2018). Routes through BS 7671 Table 41.2 via KS 1700 Annex E.
- **KE Nairobi industrial TN-S worked example** (`examples/ke-nairobi-industrial-tn-s/`) — 8 circuits including Type D compressor and 60m submain. Exercises TN-S vs TN-C-S Ze split, KS 1700 socket-RCD policy, WI3 tool deferral.
- **`validation/tool-deferral-shape.yaml`** — 2 critical checks for tool_call_pending ↔ flags pair consistency.
- **`eval-07-ke-tn-s-tool-deferral.yaml`** — 15 assertions verifying the KE example output.
- **Generator Step 9.5** — Build `zs_calc_tool_input` payload before inline Zs estimation.
- **Validator INV-9** — Enforce tool_call_pending shape consistency.

### Changed
- **Generator Step 10 reshaped** — Inline Zs estimation now sets `tool_call_pending_for_zs: true` and appends TOOL-CALL-PENDING string to top-level `flags`.
- **All 3 existing examples retrofit** — UK TN-C-S, INT TT, US NEC outputs now carry `tool_call_pending_for_zs: true` + `zs_calc_tool_input` replay payload + TOOL-CALL-PENDING string in flags. Zs numerical values unchanged from v1.0.0 (deterministic tool will refine when runtime ships).

### Notes
- Schema is backward-compatible — v1.0.0 outputs remain valid against v1.1 schema (new fields are optional).
- KS 1700 references cited as "BS 7671:2018+A2 Reg X.Y.Z (adopted by KS 1700)" to make adoption explicit.

```

- [ ] **Step 3: Commit**

```bash
git add electrical/earthing/CHANGELOG.md
git commit -m "$(cat <<'EOF'
docs(earthing): CHANGELOG v1.1.0 entry

Captures all v1.1.0 additions, the generator+validator prompt
reshape, and the existing-example retrofit pattern. Notes
backward-compatibility and KS 1700 citation convention.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 17 — Update README.md

**Files:**
- Modify: `electrical/earthing/README.md`

- [ ] **Step 1: Read current README**

Run: `head -80 electrical/earthing/README.md` and note the jurisdiction table, eval coverage table, "Tool calls awaiting runtime" table (if present).

- [ ] **Step 2: Update jurisdiction table**

Use the Edit tool. Find the existing jurisdiction table (similar to other skills' READMEs — typically a markdown table with Jurisdiction / Standards / Notes columns). Add a new KE row. Example:

```markdown
| Jurisdiction | Default standards | Notes |
|---|---|---|
| GB | BS 7671:2018+A2:2022 | TN-C-S (PME) common; TN-S legacy |
| EU | IEC 60364 | TT + TN-C-S mixed by country |
| INT | IEC 60364 | Catch-all for non-US jurisdictions outside Europe |
| **KE** | **KS 1700:2018 (adopts BS 7671 + IEC 60364)** | **TN-C-S in modern urban builds; TN-S in legacy industrial sites; TT in off-grid rural** |
| US | NFPA 70 (NEC 2023) | TN-S equivalent; supplemental grounding rods per NEC 250 |
```

- [ ] **Step 3: Update eval coverage table**

Find the eval coverage table. Change "6/3" to "7/3" if a count is shown. Add an eval-07 row:

```markdown
| Eval ID | Category | Tests |
|---|---|---|
| eval-01-uk-dwelling-tn-cs | happy_path | UK domestic TN-C-S |
| eval-02-rural-tt-system | edge_case | INT TT with electrode |
| eval-03-cpc-undersized-trap | validation_trap | CPC sizing trap |
| eval-04-missing-soil-resistivity | missing_input | Soil data absent |
| eval-05-jurisdiction-us-nec | jurisdiction_switch | US NEC bonding |
| eval-06-rationale-block | rationale_block | WI2 rationale coverage |
| **eval-07-ke-tn-s-tool-deferral** | **skill_specific** | **KE TN-S + WI3 tool deferral** |
```

- [ ] **Step 4: Add "Tool calls awaiting runtime" table**

If not already present, add the following section near the bottom of the README:

```markdown
## Tool calls awaiting runtime

| Tool name | Purpose | Status |
|---|---|---|
| `calc.electrode_resistance` | BS 7430 electrode parallel-array convergence | tool_call_pending (since v1.0.0) |
| `calc.cpc_adiabatic` | BS 7671 adiabatic CPC sizing | tool_call_pending (since v1.0.0) |
| `calc.zs_loop_impedance` | Per-circuit Zs verification (new in v1.1.0) | tool_call_pending |

Until the DraftsMan runtime ships the corresponding Python implementations, the generator emits an LLM-estimated value with a TOOL-CALL-PENDING disclaimer in `flags` and a replay payload at the IR root for deterministic re-execution.
```

- [ ] **Step 5: Update file structure section if present**

If the README has a "## File structure" section showing the folder tree, add the new files. Likely a section listing example folders — add `ke-nairobi-industrial-tn-s/` to the examples list.

- [ ] **Step 6: Commit**

```bash
git add electrical/earthing/README.md
git commit -m "$(cat <<'EOF'
docs(earthing): README — KE jurisdiction, eval-07, calc.zs_loop_impedance

Adds KE row to jurisdiction table. Updates eval coverage table to
7/3. Documents calc.zs_loop_impedance in the "Tool calls awaiting
runtime" table.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 18 — Update SKILLS_STATUS.md

**Files:**
- Modify: `SKILLS_STATUS.md`

- [ ] **Step 1: Read current earthing row**

Run: `grep -n "earthing " SKILLS_STATUS.md`
Expected: one line containing the earthing skill row.

- [ ] **Step 2: Update the earthing row**

Use the Edit tool. Find the exact current row (likely something like `| earthing | electrical/earthing | beta | BS 7671:2018, IEC 60364, NFPA 70 (NEC 2023), IEC 60617 | 6/3 ✓ | v1.0.0 schematic stage...`). Replace it with:

```markdown
| earthing | `electrical/earthing` | **beta** | BS 7671:2018, IEC 60364, NFPA 70 (NEC 2023), KS 1700:2018, IEC 60617 | 7/3 ✓ | v1.1.0 — adds KE TN-S Nairobi worked example + calc.zs_loop_impedance contract (WI3 deferred). 4 worked examples (UK TN-C-S, INT TT, US NEC, KE TN-S), 7 evals (6 WI5 + 1 skill-specific WI3+KE proof). Schema backward-compatible. |
```

- [ ] **Step 3: Validate**

Run: `grep "earthing " SKILLS_STATUS.md | head -1`
Expected: line contains "v1.1.0" and "7/3".

- [ ] **Step 4: Commit**

```bash
git add SKILLS_STATUS.md
git commit -m "$(cat <<'EOF'
docs(SKILLS_STATUS): earthing v1.0.0 → v1.1.0; evals 6/3 → 7/3

Adds KS 1700:2018 to the standards list. Notes v1.1.0 additions:
KE TN-S Nairobi example + calc.zs_loop_impedance contract.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 19 — Final code review + push

**Files:** none (review only)

- [ ] **Step 1: Run final validation across all changed files**

```bash
# Verify all JSON files are valid
python3 <<'PY'
import json, sys
files = [
    'shared/calculations/electrical/zs-loop-impedance.json',
    'shared/calculations/REQUIRED_TOOLS.json',
    'electrical/earthing/schemas/earthing-ir.schema.json',
    'electrical/earthing/skill.manifest.json',
    'electrical/earthing/examples/ke-nairobi-industrial-tn-s/input.json',
    'electrical/earthing/examples/ke-nairobi-industrial-tn-s/output.json',
    'electrical/earthing/examples/ke-nairobi-industrial-tn-s/intent-out.json',
    'electrical/earthing/examples/uk-dwelling-tn-cs/output.json',
    'electrical/earthing/examples/intl-rural-tt/output.json',
    'electrical/earthing/examples/us-commercial-nec/output.json',
]
for f in files:
    try:
        json.load(open(f))
        print(f'{f}: OK')
    except Exception as e:
        print(f'{f}: FAIL — {e}', file=sys.stderr)
        sys.exit(1)
print('---\nAll JSON files valid.')
PY
```

Expected: 10 OK lines + "All JSON files valid."

- [ ] **Step 2: Validate all 4 example outputs against v1.1 schema**

```bash
python3 <<'PY'
import json, jsonschema
schema = json.load(open('electrical/earthing/schemas/earthing-ir.schema.json'))
for ex in ['uk-dwelling-tn-cs', 'intl-rural-tt', 'us-commercial-nec', 'ke-nairobi-industrial-tn-s']:
    data = json.load(open(f'electrical/earthing/examples/{ex}/output.json'))
    try:
        jsonschema.validate(instance=data, schema=schema)
        print(f'{ex}: schema valid')
        assert data.get('tool_call_pending_for_zs') is True, 'tool_call_pending_for_zs not true'
        assert 'zs_calc_tool_input' in data, 'zs_calc_tool_input missing'
        assert any('TOOL-CALL-PENDING:calc.zs_loop_impedance' in f for f in data.get('flags', [])), 'TOOL-CALL-PENDING flag missing'
        print(f'{ex}: tool-deferral fields valid')
    except Exception as e:
        print(f'{ex}: FAIL — {e}')
PY
```

Expected: 8 lines (2 per example × 4 examples) all reporting OK.

- [ ] **Step 3: Dispatch code-reviewer agent for the full diff**

Use the Agent tool with subagent_type=code-reviewer:

```
Description: Final review of earthing v1.1.0 — KE TN-S example + calc.zs_loop_impedance contract + retrofit
Prompt: Review the full diff for earthing v1.1.0. Compare against spec docs/superpowers/specs/2026-05-17-earthing-v1.1-design.md.
Verify:
1. Schema additions are backward-compatible (no required new fields)
2. Tool deferral pattern matches WI3 precedent from arc-flash + cable-sizing + fault-level
3. KE example reasoning matches the engineering decisions outlined in spec §4
4. Eval-07's 15 assertions cover the spec §10 requirements
5. Existing 3 examples retrofit preserves Zs values unchanged
6. Validator YAML correctly references ir.flags (NOT ir.rationale.flags)
7. CHANGELOG accurately captures all changes
8. KS 1700 citations follow the "BS 7671 ... (adopted by KS 1700)" convention

Report any CRITICAL or IMPORTANT findings. Run validation commands to verify claims.

Branch: main. Files changed: ~22 (8 new + 14 modified). Commits: ~18 starting from the calc contract commit.
```

- [ ] **Step 4: Address any review findings**

If the code-reviewer reports CRITICAL issues:
1. Use Edit/Write to fix
2. Re-validate JSON + schema
3. Commit each fix
4. Re-dispatch reviewer

If the code-reviewer reports IMPORTANT issues without CRITICAL ones:
1. Fix them
2. Commit as `fix(earthing): final-review issues — <short summary>`

- [ ] **Step 5: Push to origin/main**

```bash
git push origin main
```

Expected: push success, with commit range visible.

- [ ] **Step 6: Verify push + summary**

```bash
git log --oneline -25 | head -25
```

Expected: ~18 commits visible since the v1.1 sprint started, ending with the push confirmation.

Print final summary: file count, commits, eval count promotion, what shipped.

---

## Self-review checks performed

**Spec coverage:**
- §1 Why → Task 1 (calc contract) + Task 8 (KE example) implement
- §2 Scope → file count matches Task list
- §3 Calc contract → Task 1 implements fully
- §4 KE example → Tasks 6-10 cover all 5 files
- §5 IR schema additions → Task 3 implements
- §6 Existing example retrofit → Tasks 11-13 cover all 3 examples
- §7 Generator prompt changes → Task 5
- §8 Validator prompt changes → Task 5
- §9 Validator YAML → Task 4
- §10 Eval-07 → Task 14 (assertion count is 15, slightly higher than spec's 11 — adds 4 extra assertions for completeness)
- §11 Bookkeeping → Tasks 15-18
- §12 Sequencing → matches plan's task order
- §13 Acceptance criteria → covered across Tasks 1-18
- §14 Risks → Task 11 step 4 explicitly verifies backward compat
- §15 Versioning → manifest bump in Task 15; CHANGELOG in Task 16
- §16 Out of scope → respected (no rules, no constraints, no electrode flow changes, no Python impl, no schema breaking changes, no intent consumption exercise)
- §17 Pattern parents → referenced in plan header

**Placeholder scan:** No TBD / TODO / "implement later" / "similar to Task N" patterns found. All code blocks contain full content.

**Type consistency:**
- `tool_call_pending_for_zs` (boolean, IR root) — used identically in Tasks 3, 8, 11, 12, 13, 14
- `zs_calc_tool_input` (object, IR root) — used identically across the same tasks
- TOOL-CALL-PENDING string format — exact match across Tasks 5, 8, 11, 12, 13
- `circuits[].device_type` enum — `MCB_B|MCB_C|MCB_D|BS_88_fuse|RCBO|GFCI` consistent in Tasks 3, 8, 11, 12, 13
- Validator YAML path correction (`ir.rationale.flags` → `ir.flags`) handled in Task 11 with explicit fix-commit
- `jurisdiction` enum `["GB","EU","INT","KE","US"]` — added in Task 3, used in Tasks 8 + zs_calc_tool_input subschema

Plan is internally consistent and ready for subagent execution.
